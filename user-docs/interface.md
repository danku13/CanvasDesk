---
title: Объекты интерфейса — CanvasDesk
description: Канвас, заметки, файловые карточки, группы, связи, миникарта, поиск, палитра, настройки, виджеты.
---

# Объекты интерфейса

## Канвас

Бесконечная рабочая область с сеткой. Панорамирование — Space+drag / средняя
кнопка / тачпад; зум — Ctrl+колесо к курсору (0.05×–4.0×) или pinch. Тему
(тёмная/светлая), вид и плотность сетки можно поменять в настройках
(Ctrl+,). Всё, что вы раскладываете, хранится в `.canvas`-файле — обычном
JSON [JSON Canvas 1.0](https://jsoncanvas.org/spec/1.0/).

## Заметки

Создаются двойным кликом по пустому месту. Поддерживают инлайн-редактирование
(многострочность, выделение, Ctrl+C/X/V/A, кириллица) и markdown-подмножество:
`**жирный**`, `*курсив*`, `==подсветка==`. Цвет карточки — через палитру по ПКМ
(пресеты 1–6); цвет текста подбирается автоматически так, чтобы контраст
оставался читаемым. Карточка растёт под контент; точный размер — ручной resize
за правый нижний угол. В заметках работают формулы в стиле Numi — см.
[Расчёты](calculations.html).

## Файловые карточки

Перетащите файл из проводника — карточка появится на канвасе. Превью
строится по уровням детализации: иконка → тамбнейл → превью содержимого.
Двойной клик открывает файл ассоциированным приложением; ПКМ — «Открыть
папку с файлом», «Скопировать путь», «Переименовать». Если файл недоступен
(переименован, удалён, сетевой диск отвалился) — карточка помечается серой
рамкой «битой ссылки»; верните файл на место — карточка восстановится сама
(файловый вотчер следит за изменениями).

## Группы

- Выделите несколько нод (Ctrl+клик, рамка выделения от пустого места) и
  нажмите **Ctrl+G** — вокруг них появится группа.
- Перетащите ноду на группу — она «втянется» внутрь с короткой анимацией.
- ПКМ по группе → «Действия» → «Разгруппировать».

## Связи

Связь соединяет две ноды: потяните ЛКМ от порта одной ноды к другой.
Подпись — двойной клик по линии. Хэндлы на концах выделенной связи позволяют
перепривязать её без удаления. По умолчанию связь **сама выбирает порты по
кратчайшему пути** и пересчитывает их при перетаскивании нод и автораскладке;
закрепить конец можно через ПКМ по связи → «Порты». Вид связи настраивается:
стиль (сплошная/пунктир/точки), толщина (тонкая/обычная/толстая), цвет —
через палитру связи. Опция «Связи огибают ноды» в настройках включает
маршрутизацию линий мимо чужих карточек. Связь с потоком значений
(`Shift + drag`) подсвечивается бирюзовым и показывает текущее значение —
см. [Расчёты](calculations.html).

## Поток значений: волна, меню параметра, карта потока (FR-050)

- **Волна пересчёта.** Поменяйте число в заметке-источнике — по value-связям
  вниз по потоку пройдёт короткая акцентная волна: подсветка доходит до
  каждой карточки в топологическом порядке (от ближних приёмников к дальним).
  Это визуальное подтверждение «что пересчиталось», а не анимация ради
  анимации: значения обновляются атомарно, волна лишь показывает порядок
  распространения.
- **Проливание и авто-строки.** Value-связь можно бросить прямо на параметр
  шаблонной ноды (якоря на левом краю подсвечивают совместимые по единицам
  цели) — параметр получит значение источника («проливание сильнее
  дефолта»). Если пролитое значение не читается ни одной формулой, карточка
  приёмника покажет наклонную авто-строку «Путь = значение», а наведение на
  неё раскроет полный источник («пролито: Трафик.peak_rps = 1389 rps
  (локально было: 500 rps)»). При подключении связи появится тост
  «Параметр … подтянулся из … — Ctrl+Z отменит».
- **Меню параметра.** ПКМ по пролитой строке (или по авто-строке) открывает
  меню: **«Показать источник»** — камера перелетает к заметке-истоку, связь
  подсвечивается; **«Отключить проливание»** — связь удаляется одним
  undo-шагом, параметр возвращается к локальному значению; **«Что если…»** —
  вход в режим сценариев (см. [Расчёты](calculations.html)).
- **Лейблы портов (FR-045).** Наведение на точку порта у края карточки
  показывает её адрес — полный путь «Объект.Поле»: у формульной строки —
  «Заявки.строка 2», у значения ноды целиком — «out: Заявки», у якоря
  параметра шаблонной ноды — «to: Отчёт.rps». Лейбл помогает понять, куда
  встанет связь ещё до её протягивания.
- **Карта потока (F2).** Панель справа перечисляет все проливания канваса:
  «источник → параметр = значение». Клик по строке — переход к источнику с
  подсветкой связи; панель остаётся открытой — удобно обходить несколько
  проливаний подряд. Esc или повторный F2 закрывают панель.

## Режим фокуса

Клавиша **F** (или пункт меню канваса, или настройка) включает фокус-режим:
при наведении на ноду её связи и соседи подсвечиваются, остальная сцена
притемняется. Удобно разбирать запутанные графы.

## Проверка цепочки цифры (FR-048)

Наведите курсор на любую вычисленную цифру — рядом появится **«?»**; клик по
самой цифре тоже открывает проверку. Поверх канваса разворачивается окно
проверки цепочки расчёта: дерево происхождения цифры до исходных значений
и допущений — с формулами, значениями и адресами на каждом узле. Цепочка
одновременно подсвечивается на канвасе (остальное притемняется); клик по
подсвеченной ноде канваса подсвечивает соответствующий узел дерева.

- Клик по узлу с бейджем «+N глубже» раскрывает ветку; клик по узлу с
  видимыми детьми — фокус на поддереве; чипы-крошки в шапке возвращают
  на уровень выше (клик по последнему чипу — к текущему фокусу).
- У листьев-допущений есть кнопка **«Изменить»**: подмена значения
  в моменте (what-if) с живым пересчётом канваса; панель показывает чип
  «Данные изменены», клик по нему перестраивает дерево с дельтами
  «было → стало (+Δ)».
- Кнопка **«Режим защиты»** в шапке укрупняет дерево (×1.5) для проектора:
  проза скрыта, уровни раскрываются пошагово — Space, клик по фронтиру или
  кнопки внизу окна; Esc возвращает обычный вид, повторный Esc закрывает
  окно.
- Закрытие (✕, Esc или клик по фону) не пересчитывает ничего: при
  повторном открытии той же цифры окно разворачивается мгновенно из кэша;
  если модель менялась — появится чип «Данные изменены».
- Автосвязь (фоновый детектор, тумблер в настройках) подсказывает связи
  между таблицами по совпадающим именам: предложения собираются в бейдже
  над канвасом, клик — диалог ревью, «Создать связи» — один undo-шаг.

Для ИИ-агентов те же данные доступны по MCP: `lineage` (дерево) и
`explain_number` (готовый текст «почему цифра такая»).

## Индикаторы узких мест

**Ctrl+B** (или пункт меню канваса «Узкие места», или строка «Индикаторы
узких мест» в панели настроек) включает оверлей анализа: ноды с расчётами
получают цветную рамку и бейдж с метриками — утилизация `ρ` в процентах,
время пребывания `W`, длина очереди `Q`. Жёлтая рамка — загрузка выше 70 %,
красная — выше 90 %, яркая перегрузка (`OVERLOAD 223%`) — очередь не
справляется. Анализ живой: поменяйте нагрузку в ноде — рамки и бейджи
пересчитаются сразу. Оверлей включается сам при первом появлении риска
(тост подскажет, как выключить). При сильном отдалении остаются только
рамки, а перегрузка видна с любого зума. ИИ-агент видит те же флаги
инструментом `analyze_bottlenecks` — проверяйте сценарии без UI.

## Миникарта

Правый нижний угол, 220×140. Клик — камера отцентрируется по выбранной ноде;
drag белой рамки — быстрое панорамирование. На сценах от 500 нод линии связей
на миникарте скрываются для скорости.

## Поиск

**Ctrl+F** — панель поиска. Ищет файлы по имени и содержимому (txt/md),
заметки — по заголовку и тексту, группы — по метке. Enter или клик — полёт
камеры к результату; F3 / Shift+F3 — переход между результатами. Свёрнутые
ветки mindmap в поиске не участвуют, пока не развёрнуты.

## Палитра (ПКМ по объекту)

Контекстное меню объекта собрано в группы:

- **Цвет** — пресеты 1–6 / «Без цвета»; при мультивыделении применяется ко всем.
- **Раскладка** — «Дерево →», «Дерево ↓», «Радиально»: автораскладка связанных
  нод от выбранной (связи следуют за нодами).
- **Действия** — Переименовать, Дублировать, Сгруппировать; для файлов —
  «Открыть файл», «Открыть папку с файлом», «Скопировать путь»; для заметок —
  «Очистить текст»; для групп — «Разгруппировать»; для виджетов —
  «Перезагрузить виджет», «Разрешения виджета…».
- **Ветвление** (заметки) — «Добавить дочернюю», «Добавить сиблинга»,
  «Свернуть/Развернуть ветку» — режим mindmap.
- Для связи — **Стиль**, **Толщина**, **Цвет**, **Поток** (значение/контроль),
  **Порты** (авто/закрепить исток/закрепить сток).

## Настройки

Открываются кнопкой ⚙ (угол настраивается) или **Ctrl+,**: центрированное
модальное окно над затемнённым канвасом (FR-039, стиль Obsidian). Слева —
навигация по разделам, справа — строки «название + описание + контрол»:

- **Общие** — угол кнопки настроек, HUD при старте.
- **Канвас** — сетка вкл/выкл, вид сетки (линии/точки), плотность,
  индикаторы узких мест, глубина авто-раскрытия дерева проверки цепочки
  (2/3/4/без ограничения), «Индикатор покрытия цепочками» (F-12: чип
  «Цепочки: N%» в углу канваса — доля вычисляемых цифр, чья цепочка
  доходит до листьев; по умолчанию выключен).
- **Связи и порты** — «Связи огибают ноды», зона портов (насколько «широко»
  ловится потягивание связи у ноды, 10–40 px — больше зона, легче
  целиться), «Точки выхода строк» (FR-025: порт у каждой строки расчёта,
  drag от строки — value-связь со значением этой строки; см.
  [Расчёты](calculations.html)), режим фокуса связей (как клавиша F),
  «Агрегация связей» (FR-042: пучок связей одной пары — одна линия с
  бейджем ×N; клик по ней открывает детализацию «main stage»;
  выключение возвращает прежний вид связей).
- **Внешний вид** — тема-пресет (FR-047: Nord, Dracula, Catppuccin Mocha/Latte,
  Solarized, Tokyo Night, Gruvbox — палитра применяется на лету и
  сохраняется; «Классическая» возвращает выбор карточкам «тёмная»/«светлая»),
  карточки темы «тёмная»/«светлая» и язык интерфейса (русский/English —
  применяется сразу, FR-040).

Булевы настройки — pill-тумблеры справа от строки (клик переключает).
Многозначные настройки («Угол кнопки», «Вид сетки», «Плотность сетки»,
«Зона портов», «Тема-пресет», «Язык») — кнопки со значением: клик открывает список всех
значений с галочкой у текущего; выбор — кликом или с клавиатуры (↑/↓ и
Enter), Esc закрывает только меню. Клик по затемнению вне окна закрывает
настройки целиком; открытый раздел запоминается до перезапуска. Размер
окна адаптивный — масштабируется от окна с потолками (расчёт на десктопы
Full HD+).

Тема также переключается кнопкой ☀/🌙 рядом с ⚙ — тумблер возвращает
классическую тёмную/светлую (активный пресет при этом сбрасывается).
Конфиг хранится в `~/.canvasdesk/config.toml`.

## Меню помощи «?» и документация

Рядом с кнопкой настроек и темой есть кнопка **«?»**. Она открывает меню
помощи с двумя пунктами:

- **Документация ▸** — встроенный просмотрщик справки (7 разделов, включая
  этот): панель у правого края окна. Колесо — прокрутка, ссылки между
  разделами — переход, × / Esc / клик вне панели — закрыть. Работает без
  интернета — эта же справка, что и на сайте.
- **Пройти онбординг** — повтор ознакомительного тура по функциям
  (автоматически показывается при первом запуске).

Угол кластера кнопок (⚙ / тема / «?») — один на всех, задаётся в настройках
(«Приложение» → «Угол кнопки»).

## HUD и оверлей хоткеев

- **F3** — HUD: fps, p95 времени кадра, счётчики. Полезно на больших сценах.
- **F1** — экранная шпаргалка по горячим клавишам (полный список —
  в разделе [Горячие клавиши](hotkeys.html)).

## Виджеты

JS/HTML-виджеты (календарь, стикер, часы и свои — из
[SDK](https://github.com/danku13/CanvasDesk/tree/main/sdk)): перетащите папку
виджета на канвас — виджет установится. Живой рендер — на Windows (WebView2);
на Linux/macOS виджет показывается снапшотом. Разрешения и перезагрузка —
через палитру виджета.

## Режим «вместо рабочего стола» (Windows)

Запуск `canvasdesk --desktop` встраивает канвас в рабочий стол: карточки и
заметки живут прямо на обоях, иконки рабочего стола скрываются. Повторный
запуск в обычном режиме возвращает окно. Подробности — в README репозитория.
