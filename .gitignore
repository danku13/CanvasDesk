/target
*.bak
.vscode/
default.canvas

# Локальные материалы для статей и devlog — не публиковать в GitHub
marketing/

# Node-артефакты SDK/примеров (T22): в репо только исходники
node_modules/
dist-widget/
demo-out/

# FR-036: wasm-песочница тестов canvas-core (только в wasm-прогонах)
.wasi-scratch/
crates/canvas-core/.wasi-scratch/
