# PRD-<NNNN>: <Название фичи — суть одним предложением>

> Единый шаблон PRD (Product Requirements Document). Правила ведения каталога —
> `docs/prd/README.md`. При создании нового документа скопировать этот файл,
> заменить заголовок и заполнить разделы; подсказки-комментарии `<!-- -->` удалить.
> Каждый заполненный раздел обязан быть содержательным (не «см. выше»): раздел без
> фактуры означает, что PRD к реализации не готов.

- **Статус:** <черновик / в анализе / в работе / выполнено / отклонено>
- **Тип:** PRD (документ требований продукта; реализация оформляется отдельным `FR-<NNN>` по шаблону `docs/change-requests/cr-template.md`)
- **Приоритет:** <блокер / критично / важно / желательно>
- **Владелец:** <имя агента или владельца продукта>
- **Источник:** <запрос владельца / агента / дефект: сессия, дата, дословная постановка>
- **Связанные документы:** <ADR-####, SPEC §, CR/FR, соседние PRD, interface-objects>
- **Создан:** <ГГГГ-ММ-ДД>
- **Обновлён:** <ГГГГ-ММ-ДД — при каждом изменении статуса или содержания>

---

## 1. Резюме

<!-- 2–4 абзаца: что предлагаем, кому и зачем; где проходит граница PoC.
     Читатель, открывший только резюме, должен понять суть решения и его рамки. -->

---

## 2. Контекст и проблема

<!-- Обязательны ссылки на код и документацию в формате `path/file.rs:line` и `docs/*.md §`.
     Без привязки к фактам репозитория раздел «проблема» не считается обоснованным. -->

### 2.1 Как это выглядит сегодня

<!-- Текущее поведение/архитектура: какие структуры, функции, ограничения уже есть. -->

### 2.2 Чего не хватает

<!-- Чёткий перечень отсутствующих механизмов (нет модуля / структуры / API / контракта). -->

### 2.3 Зацепки в коде и документации

<!-- Что в репозитории уже готово к использованию: паттерны, точки входа, заявленные
     «точки входа для фич» в interface-objects. Это укорачивает дорожную карту. -->

---

## 3. Цели и метрики успеха

<!-- Таблица G1..G5+: каждая метрика ИЗМЕРИМА (время, fps, %, число действий).
     «Улучшить UX» — не метрика. Метрика без способа замера — не метрика. -->

| # | Цель | Метрика |
|---|---|---|
| G1 | <цель> | <измеримый критерий> |

---

## 4. Пользователи и сценарии

<!-- 2–3 персоны с ролями и мотивацией; для каждой — что ей нужно от фичи.
     Персона 1 = основная (она же — герой CJM в §6). Не забыть про ИИ-агента (MCP),
     если фича касается контента канваса. -->

---

## 5. User stories и критерии приёмки

<!-- Формат: US-N «Как <персона>, я хочу <действие>, чтобы <ценность>».
     К каждой истории — проверяемые AC (acceptance criteria), без «работает правильно».
     AC ссылается на конкретное поведение, поверхности и лимиты. -->

### US-1. <Название истории>

> **Как** <персона>, **я хочу** <действие>, **чтобы** <ценность>.

**Acceptance criteria:**
- AC-1.1: <проверяемое условие>
- AC-1.2: <проверяемое условие>

---

## 6. CJM (Customer Journey Map) — обязательный раздел

<!-- CJM описывает путь основной персоны (§4) сквозь фичу. Два уровня: краткий (шаги)
     и детальный (мысли, эмоции, боли, точки отвала, возможности).
     Точка отвала (D#) без митигации в §8/§12 — не допускается (см. §6.3). -->

Персона CJM — **<персона из §4>**; канал — <desktop / web>. CJM зафиксирован для PoC-границы и пересматривается после первых юзабилити-прогонов (<этап дорожной карты §13>).

### 6.1 Краткий CJM (шаги)

| # | Этап | Действие пользователя | Поверхность |
|---|---|---|---|
| 1 | <этап> | <что делает> | <канвас / панель / диалог / drag-drop / вне продукта> |

### 6.2 Детальный CJM (мысли, эмоции, боли, точки отвала)

<!-- Шкала эмоций: от −2 (резко негативные) до +2 (восторг). Мысли — дословные
     реплики «внутреннего голоса». Точка отвала — формат: D#: <условие> → <реакция>. -->

| Этап | Действия | Мысли | Эмоции | Боли | Точка отвала (условие → реакция) | Возможности |
|---|---|---|---|---|---|---|
| 1. <этап> | <что делает> | «<дословно>» | <слово> (<−2…+2>) | <что мешает> | D1: <условие> → <реакция> | <идея улучшения, возможно v2> |

### 6.3 Трассировка точек отвала

<!-- Каждая D# обязана указывать на митигацию: требование F-*, метрику G*, AC или риск §12.
     Это превращает CJM из эссе в проверяемый артефакт. -->

Каждая точка отвала CJM обязана иметь митигацию в требованиях (§8) или рисках (§12): D1 → <F-*/G*/AC/§12>; D2 → …

---

## 7. Решение

### 7.1 Концептуальная схема

<!-- mermaid-диаграмма (flowchart/stateDiagram) или ascii-layout: компоненты, поток данных,
     состояния UI. Диаграмма обязательна для фич с UI и/или несколькими подсистемами. -->

### 7.2 Точка интеграции в архитектуре

<!-- Таблица «элемент → решение»: где живёт логика (крейт/модуль), какие паттерны
     репозитория переиспользуются (провайдеры, оверлеи, undo…), что сериализуется. -->

| Элемент | Решение |
|---|---|

### 7.3 Отвергнутые варианты

| Вариант | Вердикт | Причина |
|---|---|---|

---

## 8. Функциональные требования

<!-- Приоритеты MoSCoW: must (PoC без этого не существует), should, could.
     Колонка «Приёмка» ссылается на US/AC из §5, G-метрики или описывает проверку. -->

| ID | Требование | Приоритет | Приёмка |
|---|---|---|---|
| F-1 | <требование> | must | <US-N AC-N.N / G-N / описание> |

---

## 9. Нефункциональные требования

<!-- Обязательный базовый набор для этого репозитория (дополнить по вкусу):
1. Архитектура/wasm: нейтральность ядра, wasm-гейт `scripts/wasm_gate.sh` (ADR-0011),
   платформенный код за трейтами (`providers.rs`-паттерн).
2. Производительность: бюджеты против SPEC §6.3 (60 fps, старт, память).
3. Формат: JSON Canvas 1.0 не расширяется без надобности; round-trip Obsidian;
   расширения только через `canvasdesk.*` в `extra`.
4. Безопасность: секреты не сериализуются; лимиты входных данных; без сети в хосте (AGENTS.md).
5. Лицензии/зависимости: allowlist ADR-0008; обоснование новых зависимостей; native — только через ADR.
6. Детерминизм/TDD/a11y: чистые функции + тесты до реализации (AGENTS.md), контраст CR-007. -->

---

## 10. Non-goals

<!-- Явные отказы с мотивом («не X — потому что Y»). Каждый non-goal гасит будущий спор
     о скоупе; «не делаем» без причины — не non-goal, а недоработка PRD. -->

---

## 11. Открытые вопросы

<!-- Q1..Qn: вопрос, варианты ответа, влияние на решение/сроки, кто решает.
     Все вопросы, влияющие на архитектуру, закрываются до старта реализации (FR). -->

---

## 12. Риски и митигации

| Риск | Влияние | Митигация |
|---|---|---|

---

## 13. Дорожная карта

<!-- Этапы X0..Xn (буква — произвольный префикс фичи): X0 — всегда оформление
     реализационного FR (+ADR для тяжёлых зависимостей); далее — инкременты с результатами. -->

| Этап | Содержание | Результат |
|---|---|---|
| X0 | FR-<NNN> по шаблону `cr-template.md` | Разрешение на реализацию |

---

## 14. Точки входа в документацию (обязательные обновления при реализации)

<!-- Конвенция репозитория: FR без точек входа не считается полным (cr-template.md §4).
     Типовой набор: реализационный FR, SPEC.md, interface-objects/*, user-docs/ (+синк
     HOTKEYS и README/index), ACCEPTANCE.md, DEPENDENCIES.md (если зависимости),
     docs/prd/README.md (статус), docs/index.md. -->

- `docs/change-requests/fr-<NNN>-<slug>.md` — реализационный FR (создаётся на X0).
- `docs/prd/README.md` — индекс каталога PRD (статус, ссылка).
- `docs/index.md` — ссылка на этот PRD (раздел «PRD — требования продукта»).

---

## 15. Проверка (Definition of Done PoC)

<!-- Чек-лист приёмки: демо-сценарии, гейты (cargo test/clippy/fmt, wasm_gate),
     round-trip, метрики §3, закрытые точки входа §14. Пункт без способа проверки — убрать. -->

- [ ] <демо-сценарий воспроизведён>
- [ ] <гейт зелёный>

---

## 16. История изменений

<!-- Дата — кто — что изменил — статус. Каждое содержательное изменение документа. -->

- `<ГГГГ-ММ-ДД>` — <кто>: создан документ (PRD-<NNNN>), статус `<статус>`; <краткое описание>.

## 17. Источники истины

<!-- Запрос владельца; документы репозитория (ADR/SPEC/CR/FR с путями); код (`file.rs:line`);
     внешние спецификации с URL. Порядок: запрос → доки → код → внешнее. -->

- <Источник> — <что фиксирует>.
