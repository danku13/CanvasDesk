# Нода (`node`) — интерфейсный объект

Документ для последовательного расширения функционала ноды. Каждый раздел фиксирует: текущее состояние, интерфейс взаимодействия, ограничения и точки входа для новых фич.

---

## 1. Определение

Нода — базовый элемент канваса. Представляет либо файл (`type: "file"`), либо текстовую заметку (`type: "text"`), либо группу (`type: "group"`), либо виджет (`type: "widget"`, M5). Геометрия (x, y, width, height) хранится в `.canvas`-файле (JSON Canvas 1.0) и является источником истины.

**Границы ответственности:**
- Модель (`canvas-core`): структура данных, сериализация, spatial index.
- Рендер (`canvas-render`): визуализация в зависимости от LOD (zoom) и состояния (brokenLink, highlight).
- Shell (`canvas-shell`): тамбнейлы, drag-drop, файловый вотчер, оболочка (ShellExecuteEx) для файловых нод.

---

## 2. Типы нод (из `docs/SPEC.md` §5.1)

| Тип | Поля в `.canvas` | Источник данных | Жизненный цикл | Точки входа для фич |
|---|---|---|---|---|
| `file` | `id`, `file` (путь), `x`, `y`, `w`, `h`, `previewState` (`thumbnail`/`live`/`none`), `brokenLink` | Файловая система (Explorer, SMB не поддерживается полноценно) | Создаётся drag-drop (T9) или из ПКМ; удаляется пользователем; при удалении файла — `brokenLink: true`, карточка остаётся | Живое превью (M3, T11), превью PDF/Office (T12), восстановление `brokenLink` по возврату файла (T10, T16) |
| `text` | `id`, `text`, `x`, `y`, `w`, `h`, `color` (`"1"`–`"6"` или `null`) | Пользователь (инлайн-редактирование) или ПКМ-меню | Создаётся ПКМ / из текста; текст редактируется двойным кликом или фокусом | Форматирование (gfm, палитра ПКМ — T7), resize (T7), синхронизация с внешним `.md` (будущая фича) |
| `group` | `id`, `label`, `x`, `y`, `w`, `h` | Пользователь | Группирует ноды; в текущей версии визуальная группировка, без логического влияния на связи или поиск | Вложенные группы (будущая фича), сворачивание/разворачивание, фильтрация содержимого |
| `widget` (M5) | `id`, `widgetId`, `props`, `canvasdesk: { widgetId, props }` | Локальный пакет в `%APPDATA%/canvasdesk/widgets/<id>/` | Устанавливается drag папки; рендерится через WebView2; при удалении пакета — нода становится «битой» | SDK (T22, T25), permissions (манифест `widget.json`), bridge (JSON-RPC) |

---

## 3. Визуальная структура ноды (LOD из `SPEC.md` §6.2)

Нода рендерится в несколько слоёв. Каждый слой — отдельная точка входа для расширения.

```
Нода (прямоугольник с тенью и скруглением)
├── Фон (цвет типа или `color` для text)
├── Заголовок / заголовочная полоса (название файла или текст заголовка заметки)
├── Тамбнейл / превью / текст (в зависимости от LOD и типа)
├── Рамка выделения (highlight, brokenLink — серая/красная)
├── Порты связей (4: top/right/bottom/left, видны при hover)
└── Контекстное меню (ПКМ, рендерится поверх, не входит в ноду физически)
```

**LOD по zoom (`SPEC.md` §6.2):**
- `< 0.25`: прямоугольник + иконка типа файла (без текста и текстур).
- `0.25–0.6`: + системный тамбнейл (`IShellItemImageFactory`) + имя файла.
- `0.6–1.5`: + превью содержимого (картинка, первая страница PDF через `pdfium-render`, первые N строк текста).
- `> 1.5`: живое превью с прокруткой (максимум 3 одновременно, только под курсором или в фокусе).

**Для виджета (M5, `SPEC.md` §7.6):** отдельная LOD-стратегия — при `zoom < 0.25` или вне viewport → `snapshot-текстура` (`ICoreWebView2::CapturePreview`); при `zoom ≥ 0.25` и видим → живой WebView2-инстанс.

---

## 4. Взаимодействия с нодой (таблица действий)

Каждое действие — это интерфейс, который можно расширять, не ломая остальные.

| Действие | Жест / триггер | Обработчик (текущий) | Ограничение (из `SPEC.md`) | Точка входа для фичи |
|---|---|---|---|---|
| Панорамирование ноды | Drag ЛКМ (за тело или заголовок) | `canvas-app` event loop → обновление `x`, `y` в модели → автосейв (2 с); при мультивыделении (CR-001) — весь набор + дети групп | Нода не «зацепляется» за сетку принудительно; перемещение свободное | Привязка к сетке (`snap-to-grid`), выравнивание по соседям |
| Выделение | ЛКМ (одиночный); `Ctrl`/`Shift`+клик — тогл в набор; drag ЛКМ от пустого места — рамка (CR-001) | Рендер: подсветка рамки каждой; модель: `selected` (primary) + `selected_nodes` (набор, UI-state) | Выделение не сериализуется (источник истины — `.canvas`) | Постоянное выделение (`selected` в `.canvas`), фильтрация по выделению |
| Копирование / вставка / дублирование (FR-003) | `Ctrl+C` / `Ctrl+V` / `Ctrl+D` (кир.: с/м/в) | Внутренний буфер нодов; вставка центром bbox на курсор с новыми id; дубликат +32/+32 | Буфер — внутри приложения (НЕ системный clipboard); вставка связей между копиями — нет (только ноды) | Вырезание (`Ctrl+X`), копирование со связями, вставка из внешних канвасов |
| Список горячих клавиш (FR-004) | `F1` | Экранная панель слева по центру (19 записей), Esc/клик мимо — закрыть | Панель не интерактивна (строки без действий) | Интерактивные подсказки со ссылками на разделы документации |
| Редактирование текста (`text`-нода) | Двойной клик или фокус через Tab | Инлайн-редактор (glyphon + cosmic-text) поверх ноды; Enter сохраняет, Esc отменяет | Форматирование ограничено gfm (`T7`); палитра ПКМ — базовая | Расширенная палитра (код, таблицы), совместное редактирование (будущая фича), привязка к `.md`-файлу |
| Удаление | Del (клавиша) или через ПКМ-меню | Удаление из `.canvas`-модели; автосейв; связи к удалённой ноде остаются «висящими» (не удаляются автоматически) | Связи не удаляются автоматически — это осознанное решение (`SPEC.md` §5.1: связи — отдельные объекты) | Автоматическое удаление связей при удалении ноды, «мягкое» удаление (архив), восстановление через Ctrl+Z |
| Связь (`edge`) | Drag от порта (hover → 4 порта видны) → отпуск на другой ноде; перепривязка существующей связи — drag за хэндл конца выделенной связи (CR-002) | Создание `edge` в модели; перепривязка — `retarget_edge` (id/лейбл/стиль сохраняются); зона захвата портов — настраивается (CR-003: `port_zone_px` 10–40 px) | Связь не может быть «без ноды»; порты — фиксированные 4 стороны; при перепривязке в пустоту/петлю — отмена | Множественные связи между нодами, динамическая подсветка (`T23` — влито) |
| Двойной клик (`file`-нода) | ЛКМ ×2 | `ShellExecuteEx` с `SEE_MASK_INVOKEIDLIST` → ассоциированное приложение | Не работает для `text` (только инлайн-редактирование) и для `group` (пока нет поведения) | Двойной клик по `group` → «вход внутрь» (будущая фича), по `widget` → фокус виджета (а не `ShellExecute`) |
| Drag-drop (создание `file`-ноды) | Перетаскивание из Explorer (`CF_HDROP` или `FileGroupDescriptor`) на канвас | `IDropTarget` в `canvas-shell`; раскладка сеткой (5 в ряд, шаг = размер карточки + 24 px); папка → рекурсивный обход (глубина 1) | Скрытые файлы (`attrib +h`) и системные — пропускаются (`T9`, §3); `FileGroupDescriptor` (Outlook-вложения) не поддерживается | Drag папки с настройкой глубины, drag текста из браузера (уже работает — нода-заметка с текстом ссылки), drag `.canvas`-файла (импорт) |
| Контекстное меню | ПКМ по ноде или пустому месту | Меню рендерится поверх (layered-окно или поверхность wgpu); пункты: Открыть, Новый текстовый файл, Показать иконки, Выход (`T17`, §3) | Меню фиксированное; нет кастомизации | Кастомное меню через `.canvas`-конфигурацию или виджет-расширение (`T22`), пункты для конкретного типа ноды |

---

## 5. Состояния ноды (что влияет на визуал и поведение)

Каждое состояние — отдельный флаг или поле. При расширении фичи следует добавлять флаг, а не изменять существующие.

| Состояние | Где хранится | Как влияет | Точка входа для фичи |
|---|---|---|---|
| `brokenLink` (`bool`) | `.canvas` (`node.brokenLink`) + SQLite (`thumb_cache`) | Серая рамка; тултип «Файл недоступен: <путь>»; тамбнейл не обновляется; связи сохраняются | Автоматическое восстановление по возврату файла (`T10`, `T16`); ручное «перепривязать» через ПКМ |
| `selected` (временное) | `canvas-app` (UI-state, не в `.canvas`) | Подсветка рамки (цвет выделения) | Постоянное выделение в `.canvas`, выделение по фильтру (`T14`), групповое выделение связей |
| `hover` | `canvas-app` (UI-state) | Порты связей видны; курсор меняется на `pointer` для drag | Дополнительные действия при hover (быстрый превью, тултип с метаданными файла) |
| `previewState` (`thumbnail` / `live` / `none`) | `.canvas` (`node.previewState`) | Управляет LOD-рендером (`SPEC.md` §6.2) | Автоматический переход `thumbnail` → `live` при фокусе или при длительном hover; ручное «закрепить превью» |
| `color` (`text`-нода) | `.canvas` (`node.color`) | Цвет заголовочной полосы или фона; `null` → стандартный (`"1"`–`"6"` из палитры) | Дополнительные цвета, градиенты, привязка цвета к статусу (`T23`: подсветка связей) |

---

## 6. Связи (edges) как часть интерфейса ноды

Порты связей (`fromSide` / `toSide`: `top`, `right`, `bottom`, `left`) — это интерфейсный элемент ноды, хотя сами связи (`edges`) хранятся отдельно в `.canvas`.

**Текущее поведение (`SPEC.md` §5.1, `ACCEPTANCE.md` §4.1):**
- При hover на ноду появляются 4 порта (диаметр кружков следует за зоной захвата — CR-003).
- Drag от порта → резиновая линия (Безье) к курсору.
- Отпуск на другой ноде → создаётся `edge` с `fromNode`, `toNode`, `fromSide`, `toSide`, `label` (редактируется инлайн).
- Двойной клик по линии → редактирование `label`.
- Клик + Del → удаление связи.
- При перемещении ноды → рёбра перерисовываются в реальном времени.
- Перепривязка без удаления (CR-002): клик по линии выделяет связь (хэндлы-кружки на концах) → drag хэндла на другую ноду → `retarget_edge`; пустота/петля — отмена.

**Ограничения (`TASKS.md`: T8):** связи не удаляются при удалении ноды; связи не могут быть «без ноды» с одной стороны; нет множественных связей с разными `label` между одними нодами в текущей реализации (код позволяет, интерфейс не ограничивает явно).

---

## 7. Точки входа для новых фич (последовательно, без нарушения текущего функционала)

Документ составлен для расширения. Каждая строка — это будущая задача, привязанная к конкретному интерфейсному элементу ноды.

| Фича | Привязка к интерфейсу | Что нужно изменить | Где документировано как ограничение |
|---|---|---|---|
| Живое превью (`T11`) | `file`-нода, LOD `> 0.6` | `canvas-preview-host.exe` + `IPreviewHandler` + таймаут 3 с; рендер превью как текстура поверх ноды | `SPEC.md` §7.2, `ACCEPTANCE.md` §2 |
| Превью Office (`T12`) | `file`-нода, LOD `> 1.5` | Out-of-process хост (`IPreviewHandler`) + `postMessage`-мост для изоляции падений | `SPEC.md` §7.2, `TASKS.md` T11/T12 |
| Миникарта (`T13`) | Сцена целиком; ноды отображаются как упрощённые прямоугольники | `minimap.rs` + `minimap_pass.rs`; навигация: клик → центр камеры по ноде; drag viewport → панорамирование | `docs/plans/T13-minimap.md` |
| Поиск (`T14`) | Ноды (`file`: по имени + содержимому; `text`: по тексту; `group`: по `label`) | `search_ui.rs` + FTS5 (`search_index` в SQLite); панель `Ctrl+F`; анимация полёта камеры (~300 мс) | `docs/plans/T14-search.md` |
| Desktop-режим (`T15`) | Ноды рендерятся поверх обоев; `file`-ноды кликабельны через `ShellExecuteEx` | `canvas-shell/src/desktop/` + `DesktopEmbedder` (версионный гейт); иконки скрываются через `WM_COMMAND 0x7402` | `docs/plans/T15-desktop-embed.md` |
| Шина событий (`T16`) | Ноды (`file`): `brokenLink` по `Remove`/`Rename`; `text`: без изменений; `group`: без изменений | `shell_events/regs.rs`, `shfiles.rs`; каналы: `wts`, `power`, `taskbar`, `shellhook`, `clipboard` | `docs/plans/T16-shell-events.md` |
| Иконки/меню (`T17`) | Ноды (`file`): двойной клик; контекстное меню; автозапуск; краш-сейф | `icons.rs` (`fHideIcons`), `menu.rs`, `service.rs`; `sentinel`-файл для восстановления после краша | `docs/plans/T17-desktop-polish.md` |
| Энергосбережение (`T18`) | Ноды: автосейв перед `suspend`; восстановление после `resume` | Шина `power` (`suspending`/`resumed`); `powercfg /requests` — приложение не должно добавлять постоянных запросов | Не влито |
| Упаковка (`T19`) | Ноды: `.canvas`-ассоциация; автозапуск (`HKCU/Run`); MSI + Authenticode | `cargo-wix` → MSI; регистрация `ProgID`; `webview2-com` — Evergreen bootstrapper | Не влито |
| Виджеты (`T20`–`T22`) | `widget`-нода: манифест, рендер, bridge, snapshot | `canvas-widgets/src/lib.rs` (заглушка); `widget.json`; `WebView2` + `postMessage`-JSON-RPC; SDK (`assets/widgets/`) | `TASKS.md`, `SPEC.md` §7.6 |
| Подсветка связей (`T23`) | Связи (`edges`): динамическая подсветка при hover на ноду или при изменении `previewState` | Не влито; точка входа — `edgegeom.rs` или новый модуль `highlight.rs` | Не влито |

---

## 8. Проверка для каждой будущей фичи (чек-лист для расширения)

Перед добавлением фичи — пройти пункты. Это предотвращает регрессию текущего функционала.

- [ ] Фича не ломает сериализацию `.canvas` для других приложений (Obsidian) — неизвестные поля в `canvasdesk` или в `node.*` не должны нарушать `JSON Canvas 1.0`.
- [ ] Новое состояние ноды либо сериализуется, либо явно документируется как временное (UI-state, не `.canvas`).
- [ ] Новое взаимодействие с нодой не конфликтует с существующими жестами (`drag`, `hover`, `ПКМ`, `двойной клик`).
- [ ] LOD-стратегия (`SPEC.md` §6.2) учитывает новую визуальную нагрузку (текстуры > 1024² — ограничить; живые превью > 3 — ограничить).
- [ ] Фича проверена на целевой платформе (`Windows 11 25H2`, build `26200` для M4; `Windows 10 22H2` — по мере доступа).
- [ ] Журнал (`tracing`) содержит смысловой маркер для новой фичи (`ACCEPTANCE.md` §12.1 — пример для `T15`–`T17`).
- [ ] Документ `docs/interface-objects/node.md` (этот файл) обновлён: добавлен раздел или строка в таблице состояний / взаимодействий.

---

## 9. Известный дефект (2026-09-13): множественное выделение — ИСПРАВЛЕН (CR-001)

**Статус:** исправлено реализацией CR-001 (2026-09-13, см. раздел 10 и
`docs/change-requests/cr-001-selection-multi.md`). Оригинальный анализ
(для истории) ниже.

**Выявлено пользователем:** `Shift` + клик по ноде и `Shift` + `drag` (рамка) не работают для множественного выделения.

**Причина в коде (`crates/canvas-app/src/main.rs`):**
- `SceneState.selected` (`строка 190`) — `Option<Selection>` — только одна нода или одна связь.
- `on_left_button` (`строка 3182`) — `self.scene.selected = Some(Selection::Node(index));` — всегда перезаписывает, без проверки `self.modifiers.shift_key()`.
- `dragging` (`строка 192`) — `(usize, Vec2)` — одна нода + смещение; нет механизма `drag`-рамки для выделения нескольких нод.
- `delete_selected` (`строка 1031`) — удаляет одну ноду или одну связь (`match self.scene.selected` с `Some(Selection::Node(index))` или `Some(Selection::Edge(index))`), не несколько.
- Рендер (`SceneView.selected` — `строка 2339`) — передаёт один `Selection`; `renderer.render` подсвечивает одну ноду или одну связь.

**Влияние:** выделение (`selected`) — временное (`UI-state`, не сериализуется в `.canvas`), работает только для одной ноды или одной связи. Множественное выделение (`Shift` + клик, `Shift` + `drag` рамка, выделение связей при выделении ноды) — не реализовано.

**Документ-доказательство:** `docs/change-requests/cr-001-selection-multi.md` — полный анализ с ссылками на строки кода (`main.rs`: `190`, `3182`, `192`, `1031`, `2339`) и описание необходимых изменений (`SceneState.selected` → `HashSet<usize>` или `Vec<usize>`; `on_left_button` + `Shift`; `drag`-рамка; `delete_selected` для нескольких; рендер для нескольких подсвеченных нод).

**Точка входа для исправления:** раздел 6 (`Точки входа для новых фич`) — «Постоянное выделение (`TASKS.md` T14, §8)» — требует изменения структуры `selected` и рендеринга подсветки.

---

## 10. Реализованные расширения через CR/FR (2026-09-13..14)

| Запрос | Реализация | Код |
|---|---|---|
| CR-001 — мультивыделение | Рамка drag от пустого места, `Ctrl`/`Shift`+клик тогл (якорь переносится в набор — CR-001.3, ревью 2026-09-14), групповой drag, `remove_nodes` пачкой | `main.rs` (SceneState.selected_nodes), `model.rs` (remove_nodes), `lib.rs` ui (rubber_band_rect/nodes_in_rect/drag_origins/toggle_selection_with_primary) |
| CR-002 — перепривязка связей | Хэндлы концов выделенной связи, drag → `retarget_edge`, линия скрыта на время drag; перепривязка отменяема (FR-006) | `edgegeom.rs` (EdgeEnd/edge_endpoint/retarget_edge), `cards.rs` (build_edge_handle_instances) |
| CR-003 — зона портов | `Settings.port_zone_px` 10/14/20/28/40 px (панель настроек), `port_at` с параметром | `settings.rs`, `edgegeom.rs` (port_at), `cards.rs` (port_dot_diameter) |
| FR-003 — копипаст/дублирование | `Ctrl+C/V/D` (кир. с/м/в), буфер нодов, новые id, вставка центром bbox на курсор | `lib.rs` ui (reassign_ids/paste_nodes), `main.rs` (node_clipboard) |
| FR-004 — хоткеи | `F1`/пункт меню канваса (✓) — тогл панели слева по центру (22 записи; клик мимо не закрывает — FR-004.1, ревью 2026-09-14), Esc | `lib.rs` ui (HOTKEYS/hotkeys_panel_rect), `main.rs` (settings_overlay, меню канваса) |
| FR-005 — MCP node_edit | Инструмент `node_edit`: только переданные поля, label/color null — сброс; ручная приёмка — тест-долг | `canvas-mcp/src/lib.rs` (TOOLS), `main.rs` (mcp_dispatch) |
| FR-006 — undo/redo | `Ctrl+Z`/`Ctrl+Y`/`Ctrl+Shift+Z` (кир. я/н): снапшоты Canvas, стек 50, redo-ветка, все мутации (UI+MCP) отменяемы; отложенные шаги drag/resize/правки — только при фактическом изменении | `main.rs` (SceneState.undo_stack/redo_stack, App::restore_canvas/pending_undo) |
| FR-007 — вырезание | `Ctrl+X` (кир. «ч») = копирование в буфер + удаление; `Ctrl+V` — вставка; `Ctrl+Z` возвращает | `main.rs` (cut_selection) |
| FR-008 — один exe | Подкоманда `canvasdesk mcp` (stdio ↔ pipe) + автостарт сервиса; `--no-spawn` | `canvas-mcp/src/lib.rs` (run_stdio), `main.rs` (перехват подкоманды) |

Статусы и история — в `docs/change-requests/*.md` (CR-001..003, FR-003..008).

---

## 9. Источники истины (для агента, работающего с нодой)

- Формат `.canvas` — `docs/SPEC.md` §5.1; `https://jsoncanvas.org/spec/1.0/`.
- Модель данных — `crates/canvas-core/src/model.rs` (предположительно; проверить по `Cargo.toml` workspace).
- Рендер — `crates/canvas-render/src/` (`camera.rs`, `batch.rs`, `text.rs`).
- Shell-интеграция — `crates/canvas-shell/src/` (`dragdrop/`, `desktop/`, `icons.rs`, `menu.rs`).
- Планы задач — `docs/plans/T9-drag-drop.md`, `T10-file-watcher.md`, `T13-minimap.md`, `T14-search.md`, `T15-desktop-embed.md`, `T16-shell-events.md`, `T17-desktop-polish.md`.
- Приёмка (ручная) — `docs/ACCEPTANCE.md` (§4–§9, таблицы чек-листов).
- Ограничения, не являющиеся дефектами — `docs/ACCEPTANCE.md` §10.

---

*Документ составлен для последовательного расширения. Каждая новая фича должна добавлять раздел или строку в таблицу состояний, не изменяя существующие описания взаимодействий, если это не требуется для исправления дефекта.*
