# FR-061: Табличное тело ноды (Н-3, пре-PRD PRD-0004) — этап A: части значения (D-1) + замер колоночных направляющих (D-3)

- **Статус:** этапы A+B+C+D выполнены (2026-09-23). Хвосты (2026-09-23, сессия web-85edad2d, решения владельца по 15 вопросам): **hit-хвосты реализованы (коммит 1, a6c576c)** — свёрнутость блока Н-2 (runtime v1, Q4: состояние в памяти сцены, дефолт — развёрнут, сброс при загрузке), клик по заголовку (вся строка + chevron-ховер Pointer), экспандер описания «⋯ целиком ▾»/«▴ свернуть» («Раскрыть+авто»: автосворачивание при клике вне ноды/начале правки), Q3 «desc→манифест→проза» (`canvas_core::expr::first_prose_paragraph` — общая функция сцены и рендера, I-2). **Этап E реализован (коммит 2)** — kit-Row (D-15): API кита + витрина секция Row + панель FR-044 на kit-Row + единая лидер-геометрия с телом ноды; wasm +13,6 КБ ≤ 100 КБ. Q7: drag-разворот — только клик в v1 (drag после редизайна портов). Коммит 3 выполнен (2026-09-23): лестница §3.4 завершена — усечение формул узких нод (последняя ступень: алиасы Q8 → хвостовой ellipsis, перешейп тела, тултип полной формулы), Q6 (дефолт 360–400 «тяжёлым» шаблонам), Q8 (алиасы). T9: все детерминированные оракулы зелёные; VLM-ревью — визуальная приёмка владельцем (headless-скриншоты WebGPU-канваса невозможны — ограничение W4, прецедент этапа D). Порты свёрнутых строк: построчные точки исчезают вместе со строками (рёбра geometry — сторона-к-стороне, не зависят); пере-якорение слотами заголовка — v2 (нужно распределение полосы, D-7). Высота: growth-only (I-6) — свёрнутость не усаживает карточку; усечение формулы делает блок короче меры (рост-only сохраняет запас — документированная цена). Порты свёрнутых строк: построчные точки исчезают вместе со строками (рёбра geometry — сторона-к-стороне, не зависят); пере-якорение слотами заголовка — v2 (нужно распределение полосы, D-7). Высота: growth-only (I-6) — свёрнутость не усаживает карточку (цена как у FR-050 Р-4). Коммит 4 выполнен (2026-09-24, приёмка T9): паритет замер/шейпинг по весу (`TextSpec.weight`, mono=NORMAL — наложения ячеек), strip литеральных Param («имя =» слева, значение только в ячейке — задвоение), prose-фолбэк описания УБРАН (задвоение абзаца; Q3 пересмотрен по фидбэку владельца: зона = desc → манифест), экспандер описания в оценке резерва (подрез высоты), усечение Param-выражений с защищённым префиксом «имя =» + кегль плана по телу (left_size).
- **Тип:** FR
- **Приоритет:** высоко
- **Владелец:** агент сессии 2026-09-23 (этап A); этапы B–E — любой агент против замороженных контрактов
- **Источник:** `docs/interface-objects/node-tabular-body-analysis.md` (§3 спецификация с прототипа 1:1, §4 D-1…D-15, §6 этапы A–E, §5 инварианты, §9 вопросы Q1–Q9); приказ владельца «оформи CR-061 и начни этап A… важно, чтобы максимально использовался canvas-ui и вся логика и утилитарные функции правильно структурировались архитектурно, чтобы ui стал максимально декларативным» (сессия 2026-09-23)
- **Связанные задачи:** PRD-0004 (анатомия ноды — Н-3 тело); FR-013 (бейджи результата), FR-017 (what-if), FR-025 (построчные порты), FR-044 (stage-якоря/панель), FR-050 (авто-строки, инвариант 2), FR-053 (TextMeasurer F-6), FR-057/FR-058 (Painter/WidgetState/компоненты v2 — фундамент этапа E); Q9 решён владельцем 2026-09-23 (направляющие невидимы — только DebugOverlay)
- **Создан:** 2026-09-23
- **Обновлён:** 2026-09-23

## Описание
Тело ноды переводится на табличную вёрстку по спецификации прототипа (`node-tabular-body-analysis.md` §3): строка данных = ячейки имя/= /формула/лидер/значение/юнит/бейдж, числа и юниты стоят на колоночных направляющих (правое выравнивание по max-ширине по ноде), деградация ширины — лестницей §3.4, модель данных и `.canvas` не меняются. Программа из 15 доработок D-1…D-15 разбита на этапы A–E (§6). **Этап A (этот FR)** — ядро без рендера: D-1 (структурные части значения `ValueParts` — домен, canvas-core) и D-3 (замер колоночных направляющих — чистая layout-утилита, canvas-ui), мержится независимо. Этапы B (RowGrid + мультибуферный рендер), C (Н-3 режимы), D (полировка), E (kit-Row) — против замороженных ниже контрактов.

Архитектурное правило владельца (приказ 2026-09-23): максимальное использование canvas-ui; логика/утилиты — по слоям (canvas-core — домен, canvas-ui — чистые layout/измерения, canvas-render — исполнение), UI — декларативный (потребители дают данные, каркас считает геометрию).

## Влияние
| Объект | Что меняется | Где в документации |
|---|---|---|
| `expr` (canvas-core) | `Value::display_parts`, `join_parts`, `DeltaParts`/`whatif_delta_parts`; `Display` и `whatif_full_delta` — композиции над частями (байт-паритет) | анализ §4 D-1 |
| `flow` (canvas-core) | `AutoRowParts` + `AutoRow::display_parts`; `display_text` — композиция над частями (инвариант 2 FR-050) | анализ §4 D-1 |
| `row_guides` (canvas-ui, новый модуль) | `RowCellWidths`, `RowGuides::measure/with_right_edge`, `measure_row_cells` (TextMeasurer) — проход A двухпроходной раскладки | анализ §3.2, §4 D-3 |
| Потребители (этапы B–E) | `row_grid.rs`/`text.rs` — сборка ячеек и рендер по частям и направляющим; MCP-тексты — старый `Display` без изменений | анализ §4 D-2/D-4 |

## Анализ
- `canvas-core/src/expr.rs:394` — `Display for Value` уже форматирован как `{num} {unit}` (`format_num` + `unit.display()`): части выделяются без дублирования форматирования, `Display` становится композицией `join_parts(display_parts())` — единая точка сборки (байт-паритет тестом).
- `canvas-core/src/expr.rs:1821` — `whatif_full_delta` = `«{base} → {whatif} ({delta})»`: строка дельты (`whatif_delta_str`, единственная точка спец-логики «пп»/знак) не трогается; добавляется структурный вариант `whatif_delta_parts { base, new, delta }` для бейдж-колонки этапа B.
- `canvas-core/src/flow.rs:1093` — `AutoRow` хранит `path` + `Option<Value>`: `display_parts` выводится тривиально; unmapped → `num = "—"`, `unit = ""` (Р-3 диагностика сохраняется); `display_text` — композиция над частями, байт-паритет тестом (инвариант 2 FR-050: единая точка сборки).
- `canvas-ui/src/measure.rs` — `TextMeasurer::width_of` (F-6, кэш по (текст, семейство, кегль)) готов принимать замеры ячеек; направляющие — чистая функция max по строкам + арифметика правого края, без canvas-core-зависимости (G7: canvas-ui не получает новых зависимостей).
- `canvas-render/src/guides.rs` (Ф-14) — слово «guides» занято snap-осями: новый тип именуется `RowGuides` (дисциплина §5), модуль — `row_guides.rs` (canvas-ui) / `row_grid.rs` (canvas-render, этап B).
- Риски этапа A: низкие — ядро без рендера, обратная совместимость через байт-паритет `Display`/`whatif_full_delta`/`display_text` (MCP-тексты не меняются).

## Требуемые изменения
| Что | Где | Как |
|---|---|---|
| Части значения | `canvas-core/src/expr.rs` | `Value::display_parts(&self) -> (String, String)`; `pub fn join_parts(num, unit) -> String` — единственная сборка «num unit»/«num»; `Display` и `whatif_full_delta` — композиции; `DeltaParts { base, new, delta }` + `whatif_delta_parts` |
| Части авто-строки | `canvas-core/src/flow.rs` | `AutoRowParts { path, num, unit }` + `AutoRow::display_parts`; `display_text` — композиция над частями |
| Направляющие | `canvas-ui/src/row_guides.rs` (новый) + `lib.rs` export | `RowCellWidths { value_w, unit_w, badge_w }`; `RowGuides { value_w, unit_w, badge_w, value_x, unit_x }` + `measure(rows) -> Option<RowGuides>` (max по строкам) + `with_right_edge(right_edge, gap)` (x-позиции: бейдж → юнит → значение справа налево) + `measure_row_cells(measurer, fs, value, unit, badge_w, family, size)` (естественные ширины ячейки через `width_of`) |
| Тесты | оба крейта | T1-oracle частей; байт-паритет Display/whatif/AutoRow (корпус: скаляр, юнит, составной `ms·req/s`, unmapped, «пп»); T3-oracle направляющих (max по ноде, детерминизм, право-край арифметика, замер через реальный шрифт) |

## Замороженные контракты (этапы B–E и параллельные агенты кодируют против них)
```rust
// canvas-core::expr — D-1
impl Value {
    /// («800», «rps»); скаляр → (число, «»); число — как в Display (группировка/округление).
    pub fn display_parts(&self) -> (String, String);
}
/// Единственная сборка отображения значения: «num unit» | «num».
pub fn join_parts(num: &str, unit: &str) -> String;
pub struct DeltaParts { pub base: (String, String), pub new: (String, String), pub delta: String }
/// None ⇔ чтоif-дельты нет (совпадает с whatif_delta_str).
pub fn whatif_delta_parts(base: &Value, whatif: &Value) -> Option<DeltaParts>;

// canvas-core::flow — D-1
pub struct AutoRowParts { pub path: String, pub num: String, pub unit: String } // unmapped: num = "—"
impl AutoRow { pub fn display_parts(&self) -> AutoRowParts; }

// canvas-ui::row_guides — D-3 (Ф-14: имя RowGuides, не Guides)
pub struct RowCellWidths { pub value_w: f32, pub unit_w: f32, pub badge_w: f32 }
pub struct RowGuides {
    pub value_w: f32, pub unit_w: f32, pub badge_w: f32, // max по строкам (проход A)
    pub value_x: f32, pub unit_x: f32,                   // левые края ячеек (проход B ноды)
}
impl RowGuides {
    pub fn measure(rows: &[RowCellWidths]) -> Option<Self>;            // None — строк нет
    pub fn with_right_edge(self, right_edge: f32, gap: f32) -> Self;   // x от правого края тела
}
/// Естественные ширины ячеек строки: value/unit — width_of (право-выравнивание
/// считает потребитель), badge_w — passes через (пилюля/иконка).
pub fn measure_row_cells(
    measurer: &mut TextMeasurer, fs: &mut cosmic_text::FontSystem,
    value: &str, unit: &str, badge_w: f32, family: &str, size: f32,
) -> RowCellWidths;
```
Инварианты: (1) `format!("{v}") == join_parts_parts(v.display_parts())` на любом `Value` (тест-свойство); (2) текст ноды — единственный источник истины, модель/`.canvas` не меняются; (3) canvas-ui — без новых зависимостей (G7); (4) имена — Ф-14 (`RowGuides`, не `Guides`).

## Права на файлы (для параллельных агентов)
- **Этап A (эта ветка):** `crates/canvas-core/src/expr.rs`, `crates/canvas-core/src/flow.rs`, `crates/canvas-ui/src/row_guides.rs` (новый), `crates/canvas-ui/src/lib.rs` (только export), тесты обоих крейтов.
- **Этап B (следующий):** `crates/canvas-render/src/row_grid.rs` (новый), `crates/canvas-render/src/text.rs` (`with_body_stack`, сборка ячеек), `cards.rs` (пилюли-бейджи).
- **Запрещено трогать в рамках FR-061 вообще:** онбординг (`onboarding_ui.rs`, заморожен владельцем); зоны FR-059/FR-060 (`hints_ui.rs`, `flowmap_ui.rs`, `calc_panel_ui.rs`, `autolink_ui.rs`, `palette.rs`, `explain_ui.rs`, `kit_ui.rs`, хвосты app.rs dialog/menu/hotkeys); MCP-текстовые мосты (`mcp_text.rs` — остаётся на `Display`).
- **app.rs** — только этап C/D (D-13 правка, S) по отдельному согласованию.

## Зависимости
| Отношение | С кем |
|---|---|
| Кодировать | сразу — этап A зависит только от U3 (TextMeasurer, в main); волна 2 (FR-056/057/058) в main — фон |
| Сливать | независимо от FR-059/FR-060 (зоны файлов не пересекаются; canvas-core/canvas-ui против их canvas-app) |
| После этапа A | этап B (D-2/D-4/D-5/D-6/D-11 — `row_grid.rs` + мультибуферный рендер), затем C (Q1–Q4), D, E (kit-Row на `RowGuides` + Painter/WidgetState) |

## Точки входа
- `docs/interface-objects/node-tabular-body-analysis.md` — §4 (D-1/D-3 статусы), §6 (этапы), §9 (вопросы).
- `docs/change-requests/index-cr-fr.md` — статус FR-061.
- `docs/prd/README.md` — строка prd-0004 (упоминание дизайна табличного тела + FR-061).
- `worklog.md` — двойная запись этапа A.

## Проверка
- T1-oracle: «800 rps» → («800», «rps»); скаляр → юнит пуст; составной `ms·req/s`; группировка как Display; unmapped авто-строки → num «—».
- Байт-паритет: `Display`, `whatif_full_delta` (вкл. «+0 пп»-ветку), `AutoRow::display_text` — точные строки корпуса до/после.
- T3-oracle: `RowGuides::measure` = max по строкам (в т.ч. пустой список → None); `with_right_edge` — арифметика правого края (badge → unit → value, зазор gap); детерминизм (одинаковые входы → идентичные направляющие); `measure_row_cells` — реальный шрифт (NotoSansDisplay), ширina растёт с длиной, mono-базис.
- Гейты: `cargo fmt --all --check`; `CARGO_INCREMENTAL=0 cargo clippy --workspace --all-targets -- -D warnings`; `CARGO_INCREMENTAL=0 CARGO_PROFILE_DEV_DEBUG=0 cargo test --workspace`; `bash scripts/wasm_gate.sh`; `bash scripts/mcp_wasm_gate.sh` — зелёные.
- Пользовательских изменений нет (ядро без рендера) — онбординг не трогается.

## История изменений
- `2026-09-23` — агент: создан документ (приказ владельца «оформи CR-061 и начни этап A»); контракты D-1/D-3 заморожены, этапы B–E поставлены, права на файлы разведены; этап A реализуется в ветке `feature/fr-061-node-tabular-stage-a`.
- `2026-09-23` — агент: **этап A выполнен и влит** (merge e57942f: D-1 ValueParts/DeltaParts/AutoRowParts в canvas-core, D-3 RowGuides/measure_row_cells в canvas-ui, 9 тестов T1/T3, байт-паритет).
- `2026-09-23` — агент: **этапы B и C выполнены** (ветка `feature/fr-061-node-tabular-stage-b`, приказ владельца «начинай реализацию этапа B и этапа C»; основной FR-059 влился до старта — merge d701748, зоны не пересекались). Этап B: D-2 табличная модель `row_grid.rs` (RowCells/RowKind{Auto,Param,Calc,Total}/RowBadge, разбор ТОЛЬКО через `line_kind`/движок — дубля грамматики нет); D-4 мультибуферный рендер ячеек (значение на направляющей чисел, юнит на направляющей юнитов, право-прижатие `left = right − width`; левая часть строки — прежний блок тела, I-1); D-5 лидеры-пунктиры (BodyQuadKind::Leader, штрихи 2/3 px) и зебра (RowBg, прогоны ≥ 4 по соседству блоков); D-6 бейдж-колонка каскада Р-1 (проливание «← источник»/what-if Δ/ошибка «!») с лестницей деградации Text→Icon→None в проходе A (числа/юниты не деградируют); D-11 — направляющие чистая функция входов ключа кэша (отпечаток pass_key заготовлен для этапа C-режимов). Этап C: D-7 порог T (Q2-дефолт 4, `canvas_core::settings::NODE_BODY_BLOCK_THRESHOLD`) + блок-ведомость Н-2 («▸ расчёт · N строк», RU-плюрализация, вставка в общем `body_items` — I-2 measure=render); D-9 Σ узлового итога на направляющей чисел в заголовке блока; D-10 высоты — через общий стек + существующий growth-only fit (`measured_result_reserve_height`); D-13 правка — прежняя деградация (тело скрыто, живые результаты — live-буферы) подтверждена тестами. Исправление против анализа: классификация Calc-строк — по НАЛИЧИЮ исхода (line_kind смотрит с пустым окружением и переменные листа называет прозой). Спил-значение — строкой источника в ячейке значения (без обратного парсинга, риск §8). ОТЛОЖЕНО (этап D): свёрнутость блока/клик (нужны hit-зоны app.rs — отдельное согласование), зона описания+кламп D-8, ellipsis формулы, DebugOverlay направляющих. Гейты: fmt/clippy/test(52 сьюта, 0 отказов; порты/якоря — регресс без правок, I-1)/wasm/mcp-wasm — зелёные. renderer.rs: 2 руки заливок Leader/RowBg (механически необходимо, задокументировано).

- `2026-09-23` — агент: **этап D выполнен** (ветка `feature/fr-061-node-tabular-stage-d`, приказ владельца «Продолжай реализацию D этапа»). D-14 токены: `design/tokens` — `table.{guide_gap, leader_min, leader_pad, leader_dash, leader_gap, leader_h, leader_y_frac, zebra_run_min, desc_clamp_lines}` + `colors.table.guide_debug` (конвенция 0–1, как accent) + `motion.{body_block_flip_ms, body_clamp_ms}` (контракт переходов runtime v1); зеркала `tokens.rs` + parity-тесты JSON↔Rust; row_grid.rs/text.rs переведены на токены (значения дословно — I-1). Примечания: `badge_pad`/`badge_icon_pad`/`zebra_alpha` не применимы v1 (бейдж — текст без пилюли, зебра — слот темы `search_row_fill`) — пилюля бейджа отложена в этап E (kit-Row). D-14 i18n: `block_header_text_lang` (RU плюрализация / EN «line/lines»), сеттер `set_table_language` у Renderer/TextSystem (язык — глобальная настройка, не данные кадра), threading через shape_body/with_body_stack/body_items, отпечаток языка в results_key (точечный перешейп нод с таблицей); высота стека от языка не зависит (I-2 сохранён). D-14/Q9 DebugOverlay-направляющие: `BodyQuadKind::GuideDebug` — вертикали на right-краях value/unit через зону строк, хелпер `guide_debug_quads` (чистый, юнит-тест), сеттер `set_table_guides_visible`, app.rs проводит `self.debug_overlay` — в проде невидимы (решение Q9), цвет — токен (вне палитры тем, принцип debug_overlay.rs). O-5 rich-формулы: раскраска лексем формульных/параметрских строк по прототипу (функция ident+«(» — formula_fn + курсив, операторы — formula_op, переменные/числа — база); слоты темы `formula_fn`/`formula_op` (тёмная #c792ea/#666a7c, светлые темнее; пресеты — link/quote); формула шейпится БЕЗ GFM-парсинга — ПОБОЧНЫЙ ФИКС: `*` умножения больше не попадает в italic-спан markdown-парсера («a * 2 * 3»); грамматика не дублируется (оценка уже у движка — только визуальная классификация символов). D-8 описание+кламп: первая зона тела (до авто-строк и чисел), sans/приглушённый тон, кламп 2 строки по ФАКТИЧЕСКОЙ верстке (`clamp_desc_text` — бинарный поиск по словам + «…», паритет с мерой CR-012); источник Q3 v1 — `canvasdesk.desc` → описание манифеста шаблона (`template_descs`: снимок id→description в сцену и рендер; проза-фолбэк НЕ применён — дублировал бы первый абзац тела, решение за владельцем); `measure_body_height(+desc)` — I-2; `MeasuredReserveFn`/`ensure_result_reserve(+desc)` — growth-only refit (I-6); `calc_line_at` смещает hit-тест строк на высоту зоны; live-правка — зона скрыта (I-5); desc в CacheKey; `fit_template_node_height` — desc None (ленивый refit догоняет, документированная цена). Гейты ×5 зелёные (fmt/clippy/test — 52 сьюта 0 отказов/wasm/mcp-wasm). Гейт «G4-подобный линт витрины» не применим (витрина не тронута); VLM-ревью (T9) — визуальная приёмка владельцем.

- `2026-09-23` — агент (сессия web-85edad2d): **коммит 1/3 — hit-хвосты** (a6c576c; 15 вопросов владельца — решено): свёрнутость блока Н-2 — runtime v1 (`scene.block_collapsed`, Q4: дефолт развёрнут, сброс при загрузке; I-1/T5 — дефолтный рельеф канваса без изменений), клик по заголовку — вся строка + chevron-ховер (`BodyHit`/`BodyHitKind{BlockHeader,DescExpander}` — паттерн SpillHit, кэш App), экспандер описания «⋯ целиком ▾»/«▴ свернуть» + «Раскрыть+авто» (автосворачивание при клике вне ноды/начале правки), Q3 «desc→манифест→проза» (`canvas_core::expr::first_prose_paragraph` — общая функция сцены/рендера, I-2), `CacheKey.mode` (bit0 — блок, bit1 — описание — точечный reshaping). +11 тестов; гейты зелёные; push после rebase (параллельная сессия закрыла FR-060/FR-044 — этап E разблокирован).
- `2026-09-23` — агент (сессия web-85edad2d): **коммит 2/3 — этап E (kit-Row, D-15)**. canvas-ui kit.rs (только добавление, паттерн FR-058): `RowMarker` (None/Dot/Glyph), `RowParts` (декларативные данные), `RowStyle` (только слоты — потребитель переопределяет поля семантикой своей поверхности, скрытой арифметики нет), `RowOpts { leader, gap }`, `row_guides` (проход A+B по кит-строкам), `row_layout` (маркеры — зоны панели Р-4: точка x+6, текст x+18, «ƒ» x+20; право-прижатие value/unit на направляющих D-4; лидер D-5; пилюля бейджа — примечание этапа D «пилюля отложена в этап E» закрыто), `paint_row` (Painter FR-057: фон→маркер→текст→лидер→значение→юнит→бейдж), `leader_dash_rects` — ЕДИНАЯ геометрия штрихов с телом ноды: `text.rs` переведён на неё, локальные константы удалены (байт-паритет: тест `leader_dashes_match_node_arithmetic` + 335 тестов рендера зелёные). Витрина: секция Row (4 демо-строки на общих направляющих — параметр ×2 / формула с бейджем «← источник» / Σ; Normal/Zebra/Selected × RU/EN × темы), `KitDraw::paint_items` (пачка items — составные кит-виджеты). Панель FR-044 «Как считается» переведена на kit-Row: направляющие по ВСЕМ строкам колонки (направляющая значений стабильна при прокрутке — табличная семантика §3.1), лидер выключен (прототип FR-044 без лидера, `RowOpts::leader=false`), прежняя семантика (fill/border/маркер/приглушение Q3) — переопределением полей стиля; микро-отклонение: вертикальная центровка текста `size·1,1` (при scale 1 — пиксель-в-пиксель с прежней формулой «(h−12)/2»). Гейты: fmt/clippy -D warnings 0; 1566 lib + integration render 358/app 381 (T5 портов — регресс без правок); token_lint OK; wasm --check + замер raw cdylib release (процедура FR-060): 12 050 458 → 12 064 421 Б (+13 963 Б ≈ 13,6 КБ ≤ 100 КБ бюджета волны — новый компонент кита + секция витрины, не миграция 1:1); mcp-wasm --check. Остаток (коммит 3): ellipsis формулы (узкие ноды), Q6 (360–400 тяжёлым шаблонам), Q8 (алиасы), VLM-ревью T9.

- `2026-09-23` — агент (сессия web-85edad2d): **коммит 3/3 — хвосты лестницы (ellipsis + Q6 + Q8)**. row_grid.rs: `alias_idents` (Q8 «авто-обрезка имени»: идентификатор-токен (буква/_ + буквы/цифры/_, юникод) длиннее `ALIAS_IDENT_MAX`=16 — первые 13 + «…»; числа не трогаются — визуальная классификация символов, грамматика не дублируется, прецедент O-5), `RowEllipsis {display, full}` в `TablePass.ellipsis` (выровнен по строкам), `plan_row_ellipsis` — ТОЛЬКО расчётные строки (имя параметра/путь авто-строки не деградируют: «имя+числа+юниты всегда читаемы» §3.4; порядок — алиасы, затем `TextMeasurer::ellipsis` — детерминированный бинарный поиск), `pass_a(..., floor, prior)` — пол лестницы (повторный проход после перешейпа не поднимается обратно — план стабилен, без осцилляций) + prior-план (строки с планом фиксированы). text.rs: `body_items` принимает overrides (source_line→текст строки) — сегмент формульной строки замещается усечённым отображением (шейпится БЕЗ GFM-парсинга — formula-путь, O-5 подсветка лексем сохраняется); перешейп тела при активном плане (редкий путь: узкие ноды с длинными формулами; кэш D-11 — одноразово); привязка строк выделена в `row_geo` (два шейпа — одна привязка, I-1); тултип усечённой строки — `CachedRow.left_full` + `formula_ellipsis_hits` (паттерн LineErrorHit, механика FR-013 пр.4; Renderer::formula_ellipsis_hits → App, нейтральный тон, приоритет как у тултипа ошибки). templates.rs: `default_template_width` (Q6: R<T=4 → 300 как прежде, T≤R<T+4 → 360, R≥T+4 → 400; видимые ряды шаблонной ноды — параметры; существующие ноды не трогаются). Измерение (I-2): `measure_body_height` без усечения — план на стороне рендера; усечённый блок короче меры, growth-only refit сохраняет запас (документированная цена, зеркально desc None этапа D). Тесты +4: алиасы (кириллица/числа/порог), план усечения на узком теле (display влезает в дорожку, full — оригинал, стабильность повторного прохода, Param без плана), лестница ширины Q6, instantiate-ширина. Гейты: fmt/clippy -D warnings 0; test 52 сьюта 0 отказов (T5 портов — регресс без правок); wasm ступени 1–2 + mcp-wasm ступень 1 зелёные (wasmtime в среде отсутствует — как прежде); замер raw cdylib release (процедура FR-060): 12 064 421 → 12 077 323 Б (+12 902 Б ≈ 12,6 КБ ≤ 100 КБ). T9: детерминированные оракулы зелёные; headless-скриншоты WebGPU-канваса невозможны (ограничение W4, прецедент этапа D) — визуальная приёмка за владельцем.

- `2026-09-24` — агент (сессия web-85edad2d): **коммит 4 — приёмочные фиксы T9** (баг-репорт владельца: «вёрстка кривая, высота ноды не адаптируется под размеры наполнения, числа и units налезают друг на друга, есть дублирование текста»). Диагностика — CPU-прогон пайплайна таблицы prepare_titles (GPU-адаптер в среде недоступен — W4; зонд шрифтовой базы + числовая печать геометрии строк). Найдены и устранены шесть причин:
  (1) **Паритет замер/шейпинг — наложения чисел и юнитов.** TextMeasurer замерял ячейки с `Weight::MEDIUM`, а cosmic-text ищет «дефолтный» шрифт семейства только среди лиц ТОЧНОГО веса запроса (`get_font_matches` → `font_weight_diff == 0`); у Noto Sans Mono лица 400/700 — запрос 500 промахивался мимо семейства целиком в системный шрифт того же веса («rps» 19,2 px замер против 21,6 px рендер, −11 %) → направляющие заужены → юниты/значения вылезали за свои колонки. Правка: `TextSpec.weight` + `width_of_weighted`/`ellipsis_weighted` (canvas-ui), `measure_row_cells(+weight)` (контракт этапа A расширен весом — задокументировано), `MEASURE_WEIGHT = NORMAL` в row_grid (паритет `mono_attrs`). Регресс-тест `table_cell_measure_matches_mono_shaping` на ПОЛНОЙ базе шрифтов (FONT_DATA + системные decoy-лица): прежние тесты с mono-only FontSystem маскировали баг.
  (2) **Задвоение литеральных значений.** Левый блок Param-строки показывал «servers = 3» И ячейка значения «3» (прототип: paramRow = [имя][=][лидер]|[значение] — литерала слева нет). `is_literal_rhs` + `param_strip_overrides` (row_grid): левый блок замещается «servers =» тем же каналом overrides, что усечение (строки Param/Calc не пересекаются); build_rows поднят ДО первого шейпа (одна сборка на оба шейпа — I-2/D-11); мера — с теми же strip. Тест `tabular_rows_fit_and_literal_values_not_duplicated` (тела 360/300 px): состав strip-набора, leader-инвариант каждой строки, зазор значение→юнит по фактическим ширинам ячеек.
  (3) **Задвоение первого абзаца.** Prose-фолбэк зоны описания (Q3, коммит 1) рисовал первый проза-абзац ДВАЖДЫ — зона описания + тело — на каждой заметке с прозой (предупреждение этапа D «проза-фолбэк дублировал бы первый абзац» сбылось; фидбэк владельца 2026-09-24). Фолбэк УБРАН синхронно в рендере (`desc_text`) и сцене (`node_desc_text`) — зона описания = `canvasdesk.desc` → манифест шаблона; у обычной заметки зоны нет. Тест сцены заменён на `node_desc_without_prose_fallback`.
  (4) **Подрез высоты на строку.** Оценка уровня 1 (`estimated_result_reserve_height`) не учитывала строку экспандера описания (при клампе «⋯ целиком ▾»): ранний выход оставлял ноду на BODY_LINE_HEIGHT короче стека — контент вылезал за низ карточки. Правка: оценка числа строк описания по средней ширине глифа + строка экспандера при `wrapped >= CLAMP` (консервативно; уровень 2 — точный). Тест `estimate_includes_desc_expander_row`.
  (5) **Param-выражения не усекались.** План ellipsis применялся только к Calc: строка «load = <длинное выражение>» наезжала на ячейки (left_fits=false — спасать было нечем). `plan_row_ellipsis` расширен: Param с НЕ-литеральным RHS усекается с ЗАЩИЩЁННЫМ префиксом «имя =» (алиасы Q8 → хвостовой ellipsis; full — полная строка в тултип); литеральные RHS (числа) не деградируют никогда (§3.4). Тест `pass_a_truncates_param_expression_rhs_with_protected_prefix`.
  (6) **Кегль плана усечения.** План мерил левый текст кеглем ячеек (12), а тело шейпится кеглем 14 — бюджет дорожки завышен на 17 %, усечённая строка всё равно наезжала. `pass_a(+left_size)`: левый текст меряется BODY_FONT_SIZE.
  Известное ограничение (v2): авто-строки с длинным путём не усекаются (имя не деградирует; на шаблонах 360–400 помещается; пере-якорение — D-7 v2).
  Гейты: fmt/clippy -D warnings 0; test — 52 сьюта 0 отказов (1764 теста, +6 новых); wasm ступени 1–2 + mcp-wasm ступень 1 зелёные (wasmtime отсутствует в среде — как прежде); замер raw cdylib release (процедура FR-060): 12 077 323 → 12 080 602 Б (+3 279 Б ≈ 3,2 КБ ≤ 100 КБ). Визуальная приёмка — за владельцем (следующая сборка).

## Источники истины
- `docs/interface-objects/node-tabular-body-analysis.md` — §3 (спецификация), §4 (D-1…D-15), §5 (инварианты/Ф-14), §6 (этапы), §7 (T1/T3), §9 (вопросы).
- `crates/canvas-core/src/expr.rs` — `Display for Value` (394), `format_num` (364), `whatif_delta_str`/`whatif_full_delta` (1800–1825).
- `crates/canvas-core/src/flow.rs` — `AutoRow` (1093), `display_text` (1117).
- `crates/canvas-ui/src/measure.rs` — `TextMeasurer` (70), `width_of` (132).
- `crates/canvas-render/src/guides.rs` — Ф-14 коллизия имён.
