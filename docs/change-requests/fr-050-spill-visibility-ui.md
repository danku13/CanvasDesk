# FR-050: Наглядное проливание значений в UI — авто-строка приёмника, приёмные порты параметров, различение пролитых значений и живая диагностика

- **Статус:** в работе (решения владельца зафиксированы: Р-1…Р-4 — раунд 2, Р-5/Р-6 и Н1–Н9 — раунд 3, Н10 — раунд 4: (а) строка-проекция; открытых вопросов нет; реализация — по дорожной карте §Дорожная карта: этапы A и B выполнены, 2026-09-22)
- **Тип:** FR (Feature Request)
- **Приоритет:** важно (UI-контур волны PRD-0004/FR-042/FR-044/FR-045; закрывает «максимальную наглядность» из постановки FR-029)
- **Владелец:** danku13 (решения — ответы Q1–Q13 раунд 2 и Н1–Н10 + встречные Q1/Q3/Q5/Q6/Q8/Q13 раунд 3, сессии 2026-09-22); постановка — агент (перекрёстная проверка FR-029)
- **Источник:** перекрёстная проверка FR-029 с FR-014/FR-017/FR-025/FR-032/FR-033/FR-044/FR-045/FR-049 (сессия 2026-09-22): выявлено 13 расхождений; ответы владельца на уточняющие вопросы Q1–Q13 + новое требование «авто-numi-строка приёмника» (сообщение владельца, 2026-09-22: «нужно дополнить требования, что при подключении edge с проливанием данных к ноде без ожидающего порта, в принимающей ноде нужно автоматически выводить новую numi строку, чтобы сразу было видно что параметр подтянулся (задача - наглядный UX)»)
- **Связанные задачи:** FR-029 (порты значений v1 — MCP-контур, база настоящего FR), FR-014 (поток значений), FR-017 (what-if — приоритет переопределений), FR-025 (построчные порты, флаг `line_ports`), FR-032 (коды E-DOUBLE-INPUT/E-UNIT/E-PORT-UNKNOWN), FR-033 (graph_apply/param_set), FR-042/FR-044 (main stage, нотация «Объект.Поле», панель «Как считается»), FR-045 (unmapped «не подставлено», `dataref.rs`), FR-048 (lineage — дерево происхождения цифры), FR-049 (схемы на позиционных входах), CR-013 (концепция проливания), PRD-0004 (анатомия ноды), PRD-0007/FR-048 (lineage-подсветка — машинерия карты потока Н9-4)
- **Создан:** 2026-09-22
- **Обновлён:** 2026-09-22
- **Документ-шаблон:** `docs/change-requests/cr-template.md`

---

## Описание (What)

Постановка из перекрёстной проверки: реализовать FR-029 «в полной мере, чтобы
проливание значений и по связям было максимально наглядным». MCP-контур v1
выполнен (FR-029: `toParam`/`fromOutput`, секция `outputs`, `edges_list`,
`flow_recalc` v2, `graph_apply`); UI-контур, семантика приоритетов значений и
диагностика состояний не специфицированы — 13 расхождений с FR-014/017/025/
032/033/044/045/049 (журнал перекрёстной проверки, 2026-09-22). Владелец
ответил на уточняющие вопросы Q1–Q13; настоящий FR фиксирует принятые решения
(Р-1…Р-4), новое требование «авто-numi-строка приёмника» и оставшиеся вопросы
(Н1–Н10). Раунд 3 (та же дата): ответы на встречные вопросы и Н1–Н10 — Н1–Н9
закрыты (Р-5, Р-6 и подтверждения §2–§7). Раунд 4: Н10 закрыт — ответ
владельца «а»: авто-строка — строка-проекция. Открытых вопросов нет;
реализация идёт по дорожной карте A–F.

Целевые сцены наглядности (критерии «видно без чтения кода»):

1. **Подключил связь — сразу видно.** Value-ребро с проливанием подключено к
   ноде без ожидающего порта → в теле приёмника немедленно появляется новая
   numi-строка с пролитым значением (Р-4).
2. **Видно, ЧЬЁ значение.** Пролитое по связи значение визуально отличается от
   локального/ручного (Р-2 — шрифтовое различение).
3. **Видно, КТО победил.** Приоритет источников значения: what-if >
   проливание > локальный параметр (Р-1); у строки видно, кем установлено
   значение (пролито / what-if / вручную).
4. **Видно, ЧТО сломано и КАК починить.** Значение не подставлено →
   подсветка + тултип «проблема + путь решения» (Р-3).
5. **Видно распространение.** Изменил upstream → каскад обновлений downstream
   наблюдаем на канвасе (отбор владельца — Н9: пункты 1, 2, 3, 4, 6, §7).

## Решения (зафиксированы владельцем, сессии 2026-09-22 — раунды 2–3)

- **Р-1. Приоритет источников значения** (ответ Q2: «what-if перекрывает
  всё»). Порядок силы: **what-if override > проливание по связи (`toParam`) >
  локальное значение параметра**. В `propagate` значение параметра
  собирается каскадом: локальный дефолт → проливание перекрывает → what-if
  подмена перекрывает всё (механизм построчного override FR-017 — поверх,
  runtime-only, инвариант 2 FR-017 сохраняется). Что не изменилось: рёбра без
  `toParam` — позиционные слоты `$1..$N` как в FR-014; «последний побеждает»
  для дубль-рёбер — см. Н4 (блокировка при создании — решено: да, раунд 3).
- **Р-2. Визуальное различение пролитых значений** (ответ Q9: «нужно, чтобы
  различалось; можно подумать над отличием в рамках шрифта — особый литерал,
  наклонный шрифт или др.»). Значение, пришедшее по связи, отличается от
  локального. Направление — шрифтовое решение: наклонное начертание
  (italic) моно-шрифта значений (Noto Sans Mono Italic / наклонный синтез) и
  /или особый литерал-маркер перед значением; финальный вариант — на макете
  (паттерн проверки на прототипе FR-044 §Решения). Поверхности: строка
  параметра шаблонной ноды, авто-строка приёмника (Р-4), пилюли веера stage,
  группа «Переменные» панели «Как считается». Тултип строки всегда называет
  источник: «пролито: Трафик.peak_rps = 1389 rps».
- **Р-3. Диагностика «не подставлено» — подсветка + тултип** (ответ Q11:
  «да, надо подсвечивать, так же добавить тултип с пояснением по наведению,
  тултип должен говорить в чём проблема и как её можно решить (например,
  "не подставлено значение, подключите ноду с …")»). Unmapped-состояние
  (FR-045 R-3: связь есть, значения нет) подсвечивается (янтарный акцент
  analysis-слоя, пунктир порта/ребра) + тултип при наведении. **Контракт
  тултипа: ровно два пункта — (а) в чём проблема, (б) как её решить.**
  Шаблоны (RU/EN, i18n FR-040): «Значение не подставлено: связь есть, но
  источник не отдал значение (строка-источник удалена / стала прозой /
  колонка отсутствует). Подключите ноду с актуальным значением или исправьте
  строку-источник»; «Параметр rps не получает значение: выход peak_rps
  отсутствует у ноды "Трафик". Перепривяжите связь на существующий выход
  (клик по ребру → перепривязка)». Закрывает расхождение «тихая деградация
  FR-025 (молчаливый `Some(None)`) vs видимый unmapped-статус FR-045» в
  сторону видимого статуса с объяснением; молчаливая деградация остаётся
  только в невизуализируемом ядре потока.
- **Р-4. Авто-numi-строка приёмника** (новое требование владельца,
  2026-09-22). При подключении value-ребра с проливанием данных к ноде без
  ожидающего порта в принимающей ноде автоматически выводится новая numi-строка
  — чтобы сразу было видно, что параметр подтянулся. Задача — наглядный UX.
  Детали семантики и варианты — §Требуемые изменения п.1; вопрос
  «производная vs материализованная» решён — Н10-а (раунд 4): строка-проекция.
- **Р-5. Ручная правка пролитого параметра — без исключения: удалить связь +
  ввести локальное значение** (ответ Q1/Н1, раунд 3: «как будто создаёт
  исключение, которое потом будет сложно проконтролировать»). Отклонены оба
  варианта Н1 — и (а) «диалог отключения проливания» с состоянием
  «перекрыта вручную», и (б) «временный override, при пересчёте проливание
  снова перекроет». Модель: пролитая строка не редактируется напрямую
  (значение — проекция связи, Р-4/Н10-а). Изменить значение вручную = удалить
  связь (контекст-меню строки «Отключить проливание» — Н9-3, выделение ребра
  → Delete, меню ребра) → параметр возвращается к локальному значению →
  обычная правка. Сценарий «быстро посмотреть» — what-if (Р-1, база не
  мутируется) либо удалить связь и вернуть её через `Ctrl+Z`
  (создание/удаление ребра — один undo-шаг). Следствия: состояние
  «перекрыта вручную» в модель НЕ вводится (пунктирного статуса нет); undo и
  модель состояний остаются едиными с рёбрами; открытый вопрос FR-029
  «проливание vs ручная правка» закрыт окончательно.
- **Р-6. Именованный синтаксис выражений — сразу («вариант Б»)** (ответ
  Q3/Н8, раунд 3: «сразу переписываем на вариант Б» / «сразу реализовывать
  именованный синтаксис»). Формулы приёмника пишутся именованными путями
  «Объект.Поле»: `выручка := Заявки.Кол-во · Заявки.Средний_чек`. Резолв
  пути — по графу входящих value-рёбер: объект = имя ноды-истока, поле =
  `fromOutput` (имя выхода), `fromLine` → имя присваивания этой строки
  (fallback «строка N»), data-нода → колонка (FR-045 R-2); результат НЕ
  зависит от порядка рёбер. Позиционные `$1..$N` сохраняются как легаси
  (существующие `.canvas`, быстрые цепочки); display-подстановка
  (`dataref.rs`, FR-045) остаётся рендер-фолбэком для формул, где остались
  `$N`. Коллизии имён объектов — «Имя (2).Поле» (FR-044 Q4). Закрывает
  FR-044 Q1 (именованный синтаксис — сейчас, не v2); согласуется с ADR-0003
  «имя — канонический адрес порта». Изменения: грамматика `expr.rs`,
  таблица резолва имён в `flow.rs` (порядок сборки env — Р-1), миграция схем
  FR-049 (Н6).

## Требуемые изменения (Changes)

### 1. Авто-строка приёмника (Р-4)

Определение **«ожидающего порта»** — слот приёмника, готовый принять значение:
для шаблонной ноды — параметр, в который уже адресовано ребро (`toParam`-цель,
строка параметра существует); для текстовой ноды — позиционный слот `$1..$N`,
который читается формулой ноды.

Случаи срабатывания (матрица):

| Ребро | Приёмник | Ожидающий порт | Поведение |
|---|---|---|---|
| `toParam` → существующий параметр | шаблонная | да | Существующая строка параметра помечается маркером проливания (Р-2), значение перекрывает локальное (Р-1). Новая строка НЕ создаётся |
| `toParam` → параметра нет | шаблонная | нет | Ошибка `E-PORT-UNKNOWN` (MCP-валидация имён — без изменений FR-029/FR-032); UI-drag не может создать такую связь (§2) |
| value-ребро без `toParam` | текстовая | нет (формула не читает `$N`) | **Авто-строка**: «<Объект>.<Поле> = <значение> <unit>» — короткое имя поля, полный путь в тултипе (Н7), начертание Р-2 |
| value-ребро без `toParam` | шаблонная | нет | **Авто-строка**: «<Объект>.<Поле> = <значение>» (позиция слота `$N` — в тултипе: отображение `$N` на ноде запрещено FR-044 Р-3) + диагностика W-UNUSED-SLOT (FR-032) с тултипом Р-3: «значение пришло, но формула шаблона его не читает — подключите к параметру через toParam» |

Свойства авто-строки:

- **Позиция:** верх тела ноды, зона «Переменные · входящие значения»
  (FR-045 R-4 / PRD-0004 зона C), после последней строки входов.
- **Природа (решено — Н10-а, раунд 4: строка-проекция):**
  производная (display-level) — вычисляется
  в `recompute_flow` из графа связей, НЕ сериализуется в `.canvas`
  (round-trip байт-в-байт), не попадает в undo как отдельная сущность;
  повторный пересчёт даёт идентичную строку. Строка НЕ редактируется напрямую
  (Р-5): ручная правка — только после удаления связи.
- **Жизненный цикл:** появилась при создании ребра; исчезает при удалении
  ребра (конвертации в ручную нет — Р-5); при потере значения источником —
  переходит в unmapped-вид Р-3 (пунктир, тултип «не подставлено»).
- **Значение и единицы:** значение берётся из слота потока (`inbound` с
  `fromLine`/`fromOutput`/узловое); единицы — по правилам Н5.
- **Undo:** один шаг на создание связи (существующий снапшот канваса;
  производная строка не участвует).

### 2. Приёмные порты параметров — UI-механизм `toParam`-связей (Н2)

Сегодня `toParam`-рёбра создаёт только MCP (`edge_create` v2 / `graph_apply`);
UI-drag адресует ноду целиком (расхождение №2 журнала проверки). Требуется
(решение Н2 — да, раунд 3):

- У шаблонной ноды каждый параметр листа получает **входной якорь** на левом
  краю (вертикаль — уровень строки параметра, зеркально построчным выходам
  FR-025; hit-тест — допуск CR-003).
- Drag от выхода истока (построчный порт FR-025 / `outputs`-порт / порт
  data-ноды FR-045) на якорь параметра = value-ребро с `toParam`.
- Во время drag допустимые параметры подсвечиваются (unit-совместимые —
  ярче, по правилам Н5/E-UNIT); drop мимо якоря — меню выбора параметра
  приёмника либо отмена.
- Для истока-текстовой ноды без `outputs`: drag от построчного порта уже
  несёт `fromLine`; drag от ноды целиком при >1 формульной строки —
  W-AMBIGUOUS-SRC (FR-032), UI предлагает выбор строки.

### 3. Блокировка дубль-входов при создании (Н4)

Сегодня два value-ребра в один `toParam` создаются молча («последний побеждает»
+ warning в `flow_recalc`; строгий `E-DOUBLE-INPUT` — только в
`graph_validate`, постфактум; расхождение №5 журнала). Требуется
(решение Н4/ответ Q5 — да, раунд 3: UI — диалог «Заменить источник?», MCP —
вывод ошибки):

- **UI:** drop на занятый параметр → диалог «Заменить источник? Параметр rps
  уже питается от LB.out_gateway» — [Заменить] (старая связь удаляется, новая
  создаётся, один undo-шаг) / [Отмена].
- **MCP:** `edge_create`/`graph_apply[edge_create]` с `toParam`, уже занятым
  другим value-ребром → немедленная ошибка `E-DOUBLE-INPUT` (fail-fast,
  не ждать `graph_validate`); замена — явная пара delete+create в батче.
- Семантика «последний побеждает» остаётся только для легаси-файлов,
  загруженных с дублями ( + warning, как сегодня).

### 4. Приоритеты и маркер источника значения (Р-1, Р-2)

- `flow.rs`: порядок сборки `env.params` — локальные параметры → перекрытие
  `toParam`-проливанием → перекрытие what-if подменой (param-карта
  propagator FR-017). Инвариант: снятие what-if возвращает проливание,
  удаление ребра возвращает локальное значение.
- Отображение: у каждой строки значения — маркер источника: пролито (Р-2
  начертание + тултип «пролито: <Объект>.<Поле> = <значение>», при наличии
  локального — «(локально было: 500 rps)»), what-if (маркер сценария FR-017),
  ручное (обычное начертание).

### 5. Единицы и безразмерные значения (Н5 — решено: да, ответ Q6 «хорошие fallback-сценарии»)

- `OutputSpec.unit` и unit параметров остаются опциональными (расхождение №6
  журнала: значения без размерности — абстрактные цифры — легитимны).
- Безразмерное пролитое значение в параметр с единицей — трактуется в
  единицах приёмника (Numi-семантика: «500» в параметр rps = 500 rps);
  тултип показывает приведение.
- Значение с единицей в безразмерный параметр — приходит с единицей и так
  отображается.
- `E-UNIT` срабатывает только при несовместимых размерностях (ms против rps;
  конвертируемые масштабы ms/s — совместимы, как в FR-032).

### 6. Нотация «Объект.Поле» — раскладка длины (Н7 — решено: да, ответ Q8)

| Поверхность | Форма | Комментарий |
|---|---|---|
| Тело ноды, канвас (LOD-2, вне stage) | Короткая: «Поле» | Полный путь — тултип (FR-045 R-5) |
| Ребро вне stage: подпись у истока | Значение + единица: «1389 rps» | Реализовано (main stage 3abd0a4) |
| Ребро вне stage: подпись у приёмника | Короткий адрес: «→ rps» | Полный путь — тултип |
| Main stage: пилюли веера | Полный путь + значение: «Трафик.peak_rps · 1389 rps» | FR-044 Р-3 |
| Main stage: панель «Как считается», формулы | Полный путь: «выручка := Заявки.Кол-во · Заявки.Средний_чек» | Именованный синтаксис (Р-6) — формула так и написана; для легаси-`$N` — display-подстановка (фолбэк) |
| Коллизия имён объектов | «Имя (2).Поле» | FR-044 Q4 fallback |

### 7. Каскадная наглядность (Н9 — отбор владельца, раунд 3: пункты 1, 2, 3, 4, 6)

- **(1) Пульс каскада.** При изменении upstream по value-рёбрам downstream
  проходит короткая волна подсветки (порядок = топологическая сортировка
  `propagate`; анимация — `animate.rs`, длительность/затухание — токены
  `design/tokens/motion.json`).
- **(2) Тултип «откуда значение».** У строки с пролитым значением — источник
  и прежнее локальное: «пролито: Трафик.peak_rps = 1389 rps (локально было:
  500 rps)» (поверхности Р-2/Р-4).
- **(3) Контекстное меню параметра.** «Показать источник» (камера к истоку +
  подсветка связи) / «Отключить проливание» (удаляет ребро — основной путь
  ручной правки, Р-5) / «Что если…» (what-if FR-017).
- **(4) Карта потока.** Оверлей всех проливаний канваса (источник →
  параметр → значение), клик — переход. Проверка по запросу владельца
  («карта потока — один из PRD, если не ошибаюсь»): отдельного PRD «карта
  потока» НЕТ; ближайший — PRD-0007 (explain-цепочка: дерево происхождения
  ОДНОЙ цифры + подсветка цепочки, FocusView). Карта потока отличается
  охватом (все проливания сразу, без корня) и реализуется поверх той же
  машинерии подсветки рёбер (CalcHighlight, PRD-0007/FR-048) — без
  дублирования.
- **(6) Тост при авто-строке.** «Параметр rps подтянулся из
  Трафик.peak_rps — Ctrl+Z отменит» (паттерн тостов CR-016).
- **Отклонено: (5) дельта «было → стало»** у значения при пересчёте — не
  берём (дельты остаются в what-if-баре FR-017).

## Дорожная карта реализации (этапы A–F)

Порядок A → F; этап B (грамматика) параллелится с C (разные модули); E —
после D. Каждый этап закрывается коммитом с гейтами §Проверка.

- **A. Ядро семантики (`canvas-core`) — ВЫПОЛНЕН (2026-09-22):** Р-1
  порядок сборки env в `propagate` (локальные → `toParam`-проливание →
  what-if); Н4 fail-fast `E-DOUBLE-INPUT` в `edge_create`/`graph_apply` (+
  операция батча `edge_delete`, замена источника парой delete+create);
  Н5 правила единиц; Р-4 производная авто-строка в пересчёте потока
  (Н10-а — решено, раунд 4); диагностика W-UNUSED-SLOT/E-PORT-UNKNOWN —
  без изменений (FR-032).
- **B. Именованный синтаксис (Р-6) — ВЫПОЛНЕН (2026-09-22):** грамматика
  qualified-путей в `expr.rs` (`Expr::Qualified`, дефис в поле «Кол-во»,
  суффикс коллизии « (N)»/« (id)»); таблица резолва имён по графу в
  `flow.rs` (`QualifiedNames`, `Env.qualified`, регистрируются и
  позиционные рёбра, и toParam-проливания); `$N` — легаси; W-UNUSED-SLOT и
  авто-строка считают путь читающим слот; lineage (PRD-0007 X1) разворачивает
  путь через своё ребро, без ребра — терминал «не связано»; тесты резолва,
  коллизий «Имя (2)» + алиаса «Имя (node_id)», независимости от порядка
  рёбер (инвариант 6 закрыт).
- **C. UI-порты и диалоги:** Н2 входные якоря параметров + drag с подсветкой
  совместимых; Н3 `line_ports` default ON; Н4 диалог «Заменить источник?»;
  Р-3 unmapped-подсветка + тултипы «проблема + решение»; i18n RU/EN (FR-040).
- **D. Визуализация значений:** Р-2 различение пролитых (макет → выбор
  italic/литерала, паттерн проверки FR-044 §Решения); рендер авто-строки
  (Р-4); Н7 раскладка нотации; Н9-2 тултип источника.
- **E. Наглядность каскада (Н9):** пульс (1), контекст-меню параметра (3),
  карта потока (4 — поверх машинерии PRD-0007/FR-048), тост (6).
- **F. Миграции и доки:** Н6/Р-6 — формулы 6 схем FR-049 на именованные
  ссылки (`toParam`-адресация рёбер уже выполнена контентом v2, 59e6fe9);
  user-docs (`calculations.md`, `hotkeys.md`, `interface.md`).

## Влияние (Impact)

| Объект | Что меняется | Где |
|---|---|---|
| `node` | Авто-строка приёмника, входные якоря параметров, маркеры источника значения | PRD-0004, `docs/interface-objects/node.md` |
| `edge` | Подписи концов (исток — значение, приёмник — адрес), блокировка дубль-входов, unmapped-подсветка | `docs/interface-objects/edge.md` |
| `flow` | Порядок приоритетов локальное → проливание → what-if; авто-строки как производные данные пересчёта | FR-014/FR-017, `flow.rs` |
| `expr` | Именованный синтаксис «Объект.Поле» в формулах (Р-6); `$N` — легаси | FR-044 Q1, `expr.rs`, `user-docs/calculations.md` |
| `settings` | `line_ports` default ON (Н3 — решено: да) | FR-025/FR-026 |
| `schemes` | Формулы 6 схем FR-049 — именованные ссылки (Н6+Р-6); `toParam`-адресация рёбер уже в контенте v2 (59e6fe9) | FR-049, `assets/canvas-schemes/` |
| `mcp` | `edge_create` fail-fast `E-DOUBLE-INPUT` (Н4) | FR-032/FR-033, SPEC §MCP |
| `i18n` | Тексты тултипов Р-3, диалога Н4 «Заменить источник?», меню параметра Н9-3, тостов — RU/EN | FR-040 |
| `.canvas` | **Схема формата не расширяется** — авто-строка/unmapped/маркеры — производные (Н10-а — решено, раунд 4); именованные пути в формулах (Р-6) — пользовательский текст, схема не меняется | SPEC §5.1 |

## Инварианты тестируемости

1. **Приоритет (Р-1):** параметр с локальным 500 rps, `toParam` приносит
   1389, what-if override 2000 → пересчёт 2000; снятие what-if → 1389;
   удаление ребра → 500.
2. **Авто-строка (Р-4):** подключение value-ребра к ноде без ожидающего порта
   → в теле приёмника ровно одна новая строка с qualified-именем и значением;
   повторный `recompute_flow` — строка идентична; удаление ребра — строка
   исчезла (правка строки до удаления невозможна — Р-5); round-trip
   `.canvas` байт-в-байт (строка не сериализуется — Н10-а, решено).
3. **Дубль-входы (Н4):** после успешного создания в `canvas.edges` нет двух
   value-рёбер в один `toParam`; вторая попытка — диалог (UI) / ошибка
   `E-DOUBLE-INPUT` (MCP).
4. **Тултип Р-3:** каждый unmapped-слот имеет тултип с (а) причиной и
   (б) путём решения; полнота RU/EN таблиц — инвариант FR-040.
5. **Различение (Р-2):** строка с пролитым значением визуально отличается от
   локальной (скриншот-эталон пары строк); тултип называет источник.
6. **Именованный синтаксис (Р-6):** формула `выручка := Заявки.Кол-во ·
   Заявки.Средний_чек` резолвится по именам независимо от порядка рёбер —
   перенумерация `canvas.edges` не меняет значений; легаси-`$N`-формула в
   том же файле считается по-прежнему; коллизия имён разрешается
   «Имя (2).Поле».

## Открытые вопросы (Н1–Н10; после раунда 4 открытых нет)

| # | Вопрос | Статус |
|---|---|---|
| Н1 | Ручная правка параметра с активным проливанием: (а) диалог «отключить проливание и ввести локальное значение» — связь становится «перекрыта вручную» (пунктир, тултип), повторное подключение возвращает проливание; (б) правка = временный override, при следующем пересчёте проливание снова перекроет («проливание сильнее»). Рекомендация — (а): предсказуемо и видно в UI. | **Решено (раунд 3) — Р-5:** оба варианта отклонены («создаёт исключение, которое сложно проконтролировать»); правка = удалить связь и ввести локальное значение; «быстро посмотреть» — `Ctrl+Z` или what-if |
| Н2 | UI-механизм `toParam`: входные якоря параметров + drag от выхода истока (§2). Да/нет | **Решено (раунд 3): да** |
| Н3 | Включить построчные порты (`line_ports`) по умолчанию (сегодня default OFF по исходному решению FR-025). Да/нет | **Решено (раунд 3): да** — default ON |
| Н4 | Блокировка дубль-входов при создании (§3). Да/нет | **Решено (раунд 3, ответ Q5): да** — UI: диалог «Заменить источник?»; MCP: вывод ошибки `E-DOUBLE-INPUT` |
| Н5 | Безразмерные значения: трактовка в единицах приёмника; `E-UNIT` — только несовместимые размерности (§5). Да/нет | **Решено (раунд 3, ответ Q6): да** — «хорошие fallback-сценарии» |
| Н6 | Перевести 6 встроенных схем FR-049 с позиционных `$in` на `toParam`-адресацию. Да/нет | **Решено (раунд 3): да** (+ именованные ссылки — Р-6). Уточнение после синхронизации с main: `toParam`-адресация рёбер уже выполнена контентом v2 схем (59e6fe9) — остаётся перевод формул на именованные ссылки |
| Н7 | Раскладка длины нотации по поверхностям (§6). Да/нет | **Решено (раунд 3, ответ Q8): да** |
| Н8 | Display-подстановка имён в формулах (в `.canvas` остаётся `$1 · $2`, на экране «Заявки.Кол-во · Заявки.Средний_чек») vs настоящий именованный синтаксис выражений (FR-044 Q1). Да/нет | **Решено (раунд 3, ответ Q3): Р-6 — именованный синтаксис сразу («вариант Б»)**; display-подстановка — легаси-фолбэк для `$N` |
| Н9 | Дополнения наглядности (ответ на Q13): (1) пульс каскада по рёбрам downstream при изменении upstream; (2) тултип «откуда значение» с прежним локальным; (3) контекстное меню параметра: «Показать источник» (камера к истоку + подсветка связи) / «Отключить проливание» / «Что если…»; (4) карта потока — оверлей всех проливаний канваса (источник → параметр → значение), клик — переход; (5) дельта «было → стало» у значения при пересчёте; (6) тост «Параметр rps подтянулся из Трафик.peak_rps — Ctrl+Z отменит». Отбор: какие берём? | **Решено (раунд 3): пункты 1, 2, 3, 4, 6** (п. 5 «дельта было→стало» — отклонён); проверка «карта потока = PRD?» выполнена — отдельного PRD нет, см. §7 |
| Н10 | Авто-строка — «настоящая» или «проекция»? Простыми словами: **(а) строка-проекция** — видна только на экране: в файл `.canvas` не пишется, при удалении связи исчезает, редактировать её нельзя (как подпись значения на ребре); **(б) настоящая строка** — дописывается в текст ноды и сохраняется в `.canvas`, редактируется как обычная numi-строка, при удалении связи остаётся как локальное значение. Рекомендация — (а): файл чистый, единственный источник истины — ребро, round-trip байт-в-байт; согласуется с Р-5 (строку нельзя редактировать — она и не «настоящая»). | **Решено (раунд 4, ответ владельца «а»): строка-проекция** — производная экрана, в `.canvas` не сериализуется (round-trip байт-в-байт), при удалении связи исчезает, напрямую не редактируется (согласуется с Р-5); рабочее допущение ядра подтверждено |

## Проверка (Verification)

- **Наглядная проверка (5 минут, демо-критерий):** на эталоне ADR-0005 —
  подключить ребро «Трафик.peak_rps → Gateway» (Gateway без ожидающего порта)
  → в теле Gateway немедленно видна строка «Трафик.peak_rps = 1389 rps»
  (наклонное начертание Р-2); изменить DAU в «Трафик» → строка и итог
  Gateway обновились; перекрыть what-if → маркер сценария; удалить ребро →
  строка исчезла, Gateway вернулся к локальному значению. Всё наблюдаемо без
  чтения кода.
- Юнит: инварианты 1–5 (фикстуры по каждому; детерминизм авто-строки;
  fail-fast MCP).
- Гейты: `cargo fmt --all`; `cargo clippy --workspace --all-targets --
  -D warnings`; `cargo test --workspace`; `scripts/wasm_gate.sh`.

## История изменений (Changelog)

- `2026-09-22` (4) — агент (этапы A–B дорожной карты, ядро без UI):
  **A** — Р-1 каскад приоритетов в `propagate_with_lines_data` (инвариант 1);
  Н5 безразмерные в единицах приёмника (`spill_value_in_param_units`,
  `dimensions_compatible`); Н4 fail-fast `E-DOUBLE-INPUT` (прямой
  `edge_create` и батч; новая операция `edge_delete {id|ref}` + ref-ы рёбер
  в карте батча — замена источника парой delete+create, один undo-шаг);
  Р-4 производная авто-строка (`AutoRow`/`auto_rows`, кэш
  `SceneState.auto_rows` в `recompute_flow`). **B** — Р-6 именованный
  синтаксис: `Expr::Qualified {obj, field}` + лексер (дефис в поле
  «Кол-во», суффикс коллизии « (N)»/« (id)» с нормализацией пробела);
  `Env.qualified` + `EvalError::UnknownInput` («вход не найден:
  Объект.Поле» — видимая ошибка, не проза); таблица резолва
  `QualifiedNames` в `flow.rs` (уникальное имя → «Заявки»; коллизия:
  первая — «Заявки», N-я — «Заявки (N)», все одноимённые — алиас «Заявки
  (node_id)» — зеркала dataref; регистрируются позиционные рёбра И
  toParam-проливания); W-UNUSED-SLOT/авто-строка: путь читает слот своего
  ребра; lineage X1: путь → своё ребро → строка-источник, без ребра —
  терминал «не связано». Тесты: +12 (парсер 2, flow 8, lineage 1, validate
  1) — инвариант 6 закрыт (порядок рёбер, легаси-`$N`, «Имя (2)»);
  гейты fmt/clippy/test/wasm/mcp-wasm зелёные (550 нативных, 466 под
  wasmtime, e2e oracle ±1 %).
- `2026-09-22` (3) — агент (раунд 4 — ответ владельца по Н10): Н10 закрыт —
  ответ «а», авто-строка приёмника = **строка-проекция**: производная экрана,
  в `.canvas` не сериализуется (round-trip байт-в-байт), при удалении связи
  исчезает, напрямую не редактируется (согласуется с Р-5). Рабочее допущение
  реализации ядра подтверждено владельцем; формулировки «рабочее допущение»
  по всему документу заменены на «решено — Н10-а». Открытых вопросов нет —
  статус «в работе» (реализация по дорожной карте A–F).
- `2026-09-22` (2) — агент (раунд 3 — ответы владельца на Н1–Н10 и встречные
  Q1/Q3/Q5/Q6/Q8/Q13): закрыты Н1–Н9, открыт только Н10 (переформулирован
  простыми словами). Новые решения: **Р-5** — правка пролитого параметра =
  удалить связь + ввести локальное значение, `Ctrl+Z` возвращает связь;
  состояния «перекрыта вручную» нет (оба варианта Н1 отклонены). **Р-6** —
  именованный синтаксис выражений «Объект.Поле» сразу («вариант Б»);
  display-подстановка — легаси-фолбэк для `$N`; FR-044 Q1 закрыт.
  Подтверждены: Н2 (якоря), Н3 (`line_ports` ON), Н4/Q5 (диалог «Заменить
  источник?» + MCP `E-DOUBLE-INPUT`), Н5/Q6 (безразмерные в единицах
  приёмника), Н6 (миграция схем + Р-6), Н7/Q8 (раскладка). Н9/Q13 — отбор
  1, 2, 3, 4, 6 (дельта «было→стало» отклонена); проверка «карта потока =
  PRD?» — отдельного PRD нет, ближайший PRD-0007 (§7). Матрица §1: авто-строка
  шаблонной ноды — «<Объект>.<Поле>» без «вход $N» (FR-044 Р-3). Добавлены
  дорожная карта A–F и инвариант 6 (резолв имён). Статус → «в работе».
- `2026-09-22` — агент: создан по перекрёстной проверке FR-029 (13
  расхождений с FR-014/017/025/032/033/044/045/049). Зафиксированы решения
  владельца (Q2→Р-1 «what-if перекрывает всё»; Q9→Р-2 различение шрифтом/
  литералом; Q11→Р-3 подсветка + тултип «проблема + решение»; новое
  требование→Р-4 авто-numi-строка приёмника). Оставшиеся расхождения —
  вопросы Н1–Н10 (после перезапуска сессии номера Q3/Q4/Q7/Q10/Q12
  восстановлены по журналу — помечены «предварительно да, подтвердить»).
  v2-контур UI FR-029 передан настоящему FR.

## Источники истины (References)

- `docs/change-requests/fr-029-value-ports.md` — порты значений v1 (MCP),
  открытый вопрос «проливание vs ручная правка» → Н1.
- `docs/change-requests/fr-017-what-if-scenarios.md` — построчный override
  (верх приоритета Р-1), инвариант 2 runtime-only.
- `docs/change-requests/fr-025-line-output-ports.md` — флаг `line_ports`
  (default OFF → Н3), построчные порты, паттерн входных якорей §2.
- `docs/change-requests/fr-032-graph-read-validate.md` — E-DOUBLE-INPUT/
  E-UNIT/E-PORT-UNKNOWN, W-AMBIGUOUS-SRC, W-UNUSED-SLOT.
- `docs/change-requests/fr-044-mainstage-fan-readability-qualified-refs.md` —
  Р-3 (уточнён решением Р-6: именованный синтаксис — сразу, display —
  легаси-фолбэк), пилюли веера, панель «Как считается» (Н7/Н8); Q1 закрыт.
- `docs/change-requests/fr-045-node-anatomy-data-source-unmapped-calc-trace.md`
  — R-3 unmapped «не подставлено», R-4 группы «Переменные»/«Расчёт», R-5
  «Объект.Поле» (Р-3/Р-4/Н7).
- `docs/change-requests/fr-049-canvas-scheme-templates.md` — схемы на
  позиционных входах (Н6).
- Ответы владельца Q1–Q13 + новое требование (сессия 2026-09-22, чат).
