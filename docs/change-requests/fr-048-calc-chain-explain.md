# FR-048: Цепочка расчёта цифры — lineage-ядро, explain-окно, what-if из дерева, автосвязь, режим защиты, MCP (PRD-0007)

- **Статус:** в работе (X0–X2 выполнены 2026-09-22 — FR создан, ядро lineage, окно проверки/триггер/подсветка; демо-гейт Q6; далее X3 — what-if из дерева)
- **Тип:** FR (Feature Request)
- **Приоритет:** критично
- **Владелец:** danku13 (владелец продукта)
- **Источник:** PRD-0007 (`docs/prd/prd-0007-calc-chain-explain.md`, Q1–Q8
  закрыты — §11); решение владельца «реализуем PRD-0007» (сессия 2026-09-22).
  Открытые вопросы PRD закрываются предложенными вариантами по умолчанию:
  Q2 — матч автосвязи по точным именам присваиваний, совпадение единиц —
  бонус-признак в ревью; Q3 — «?» у цифры без входов открывает панель одного
  узла «исходное значение» (AC-1.4, триггер никогда не глушится); Q4 —
  режим защиты запускается тумблером в шапке окна проверки (одним
  действием, AC-6.1); глобальный хоткей — v2 (точки входа `HOTKEYS`/
  `user-docs/hotkeys.md` сохраняются); Q6 — демо-гейт владельца после X2.
- **Связанные задачи:** PRD-0007 (§7 решение, §8 F-1..F-12, §9.4 тесты,
  §13 дорожная карта X0–X6, §15 DoD); FR-014 (flow — только чтение),
  FR-013/FR-025/FR-029 (строки/порты/адресация), fr-044/fr-045 (панель
  «Как считается», R-3/R-5), FR-042 (пучки/main stage, `EdgeBundleIndex`),
  FR-017 (what-if сценарии — переиспользуется целиком), FR-006 (undo-баты),
  FR-032/FR-033 (MCP), FR-039 (настройки — лимит глубины, тумблер
  автосвязи), FR-040 (i18n), FR-046 (токены), FR-049 (паттерн реализации
  PRD → FR), `docs/prototypes/ux-defense-mode.html` (v4 — эталон окна),
  `docs/prototypes/ux-review-dialog.html` (У7 — эталон ревью до решения)
- **Создан:** 2026-09-22
- **Обновлён:** 2026-09-22
- **Документ-шаблон:** `docs/change-requests/cr-template.md`

---

## Описание (What)

PRD-0007 требует «цепочку расчёта любой цифры»: у любой вычисляемой цифры
(итог ноды, построчный результат) появляется триггер «?» и фолбэк-клик по
цифре, открывающие плавающее окно проверки поверх полноэкранного канваса
с полным рекурсивным деревом происхождения до листьев-констант/допущений,
синхронной подсветкой цепочки на канвасе с затемнением остального,
подменой листа-допущения через рантайм what-if FR-017, автосвязью таблиц
по совпадающим именам присваиваний и режимом защиты (состояние Defense
того же окна: ×1.5, пошаговое раскрытие, скрытие прозы). MCP-агент получает
текстовую выдачу объяснения (`explain_number`). Реализуется по дорожной
карте §13: X0 (этот FR) → X1 lineage-ядро → X2 триггер+окно+подсветка →
X3 what-if из дерева → X4 автосвязь → X5 режим защиты → X6 интеграция/MCP.

## Влияние (Impact)

| Объект/подсистема | Что меняется | Где в документации |
|---|---|---|
| `canvas-core` | новые чистые модули `lineage.rs` (X1) и `autolink.rs` (X4); `flow.rs` — только чтение (визуальность `spill_source_title`) | PRD-0007 §7.1/§7.2, §9.1 |
| `canvas-render` | триггер «?» у полосы D/построчных рядов; оверлей `CalcHighlight` (подсветка+затемнение, доминантное ребро пучка) | PRD-0007 §7.2 (F-1/F-4) |
| `canvas-app` | окно проверки (паттерн main stage), честный лоадер, сессионный кэш снапшота, чип «Данные изменены», подмена листа, сценарии, ревью автосвязи, Defense | PRD-0007 §6.4–6.5, §7.2 |
| `canvas-web`/MCP | `explain_number` (F-9, must) | PRD-0007 §7.2 |
| Формат `.canvas` | НЕ расширяется (G6) — происхождение вычисляется, сценарии в `canvasdesk.whatif` | PRD-0007 §9.3 |

## Анализ (Root Cause — чего нет в коде)

- Нет lineage-модели: `value_path` (`flow.rs:870`) — только кратчайший путь
  между нодами, без дерева, строк и значений; `inbound_slots_with_lines`
  (`flow.rs:555`) дают входы одной ноды без рекурсии.
- Нет триггера и окна объяснения: полоса D и построчные ряды не имеют
  hit-зон «?»; main stage (fr-044) ограничен парой нод пучка.
- Нет what-if из дерева: override-цели `(node, line)` адресуются только из
  бара FR-017.
- Нет детектора автосвязи: `line_kind` (`expr.rs:1719`) есть, но скан
  совпадающих имён присваиваний между нодами и ревью-диалог отсутствуют.
- Нет режима защиты: укрупнение ×1.5 и пошаговое раскрытие существуют
  только в HTML-прототипе v4.

## Требуемые изменения (Changes)

| Этап | Что → где → как |
|---|---|
| X1 | `canvas-core/src/lineage.rs`: `LineageNodeId`/`LineageNode`/`LineageChild`/`LineageVia`/`LineageTree`/`LineageError`/`LineageFlow` + `build_lineage(canvas, flow, root)` — рекурсивный upstream-обход (обобщение `value_path`): value-рёбра (слоты/проливания/`fromOutput`), локальные переменные Numi-листа (последнее присваивание до строки), спиллы, unmapped-входы, циклы — терминальные узлы; бюджет `LINEAGE_MAX_NODES`; тесты §9.4 |
| X2 | Триггер «?» (render), окно проверки (паттерн main stage, движок `ux-defense-mode.html` v4), честный лоадер (12 ключей i18n), `CalcHighlight`, сессионный кэш + чип, лимит глубины в FR-039 |
| X3 | Подмена листа из дерева → `WhatIfOverrides` (`flow.rs:235`), дельты, именованные сценарии, таблица сравнения, Apply одним undo-шагом |
| X4 | `canvas-core/src/autolink.rs`: детектор точных имён присваиваний (фильтры: циклы `creates_value_cycle`, дубликаты), фон с дебаунсом, бейдж, диалог ревью, undo-бат, тумблер FR-039 |
| X5 | Состояние Defense окна проверки: ×1.5 из токенов, пошаговое раскрытие, скрытие `NumiLineKind::Prose`, выход без следов канваса |
| X6 | MCP `explain_number` (линейная развёртка дерева), взаимоисключимость оверлеев (реестр F-10), аудит регрессий, доки §14, приёмка G1–G7 |

## Точки входа

- `docs/SPEC.md` §5.1 (не расширяется — фиксация), §6.3 (бюджеты дерева), §8 (модальности).
- `docs/interface-objects/node.md` §3/§4 (триггер «?»); `edge.md` §3/§4 (подсветка цепочки).
- `docs/prd/README.md`, `docs/index.md` — статус PRD-0007.
- `user-docs/interface.md`, `user-docs/hotkeys.md`, `docs/ACCEPTANCE.md` — приёмка PoC.
- `docs/change-requests/index-cr-fr.md` — запись FR-048.

## Проверка

- Юнит-тесты `build_lineage` — все случаи §9.4 (линейная цепочка, ромб,
  переменные листа, спиллы, unmapped, цикл, лист-константа, корень-строка
  vs корень-итог, проза-корень, нода без результата, 1000+ узлов) — зелёные.
- `cargo test --workspace`, clippy `-D warnings`, fmt — зелёные;
  `scripts/wasm_gate.sh`, `scripts/token_lint.sh` — зелёные.
- По этапам X2–X6 — чек-листы §15 (DoD) и демо-гейты Q6.

## История изменений

- `2026-09-22` — агент: создан документ (X0) по PRD-0007; Q2/Q3/Q4/Q6
  закрыты вариантами по умолчанию; статус `в работе` — начат X1.
- `2026-09-22` — агент: **X1 выполнено.** `canvas-core/src/lineage.rs`:
  модель (`LineageNodeId`/`LineageNode`/`LineageChild`/`LineageVia`/
  `LineageTree`/`LineageError`/`LineageFlow`, `LINEAGE_MAX_NODES` = 4096) +
  `build_lineage(canvas, flow, root)` — итеративный DFS с явным стеком
  задач (Enter/Terminal/Exit; рекурсия отброшена после переполнения стека
  на цепочке 1005 нод — глубина ограничена кучей, §9.2/G5); режимы Ready
  (`FlowSolutions` + `DataSnapshots`) и Cycled (`CycleError` — топология
  без значений, AC-2.4); проекция: value-рёбра (слоты/`$in`/проливания
  `toParam` — сильнее дефолта/`fromOutput` — последняя присваивающая
  строка текстовой ноды или секция outputs шаблона; приоритет `fromLine`
  — зеркало `edge_source_value`), переменные Numi-листа (последнее
  присваивание ДО строки, фенсы — как в `eval_lines_with_env`), CSV-колонки
  data-нод — листья со значением ячейки; терминалы «цикл»/«значение не
  подставлено»/«не связано»; бюджет с ленивым общим маркером усечения;
  ромб без дедупликации; порядок детей — `canvas.edges`. Движок не менялся:
  `flow.rs` — только `spill_source_title` → `pub(crate)`. Тесты §9.4 (15)
  — зелёные; cargo test canvas-core (320 lib-тестов + интеграционные),
  clippy `-D warnings`, fmt, `cargo check --workspace` — зелёные.
- `2026-09-22` — агент: **X2 выполнено** (окно проверки + триггер +
  подсветка + честный лоадер + кэш). `canvas-app/explain_ui.rs` — модель
  окна (геометрия по прототипу v4 min(88vw,1320)/min(86vh,900), инвариант
  320×240; видимость AC-2.3 — авто до лимита + ручное; tidy-лейаут
  колонка=уровень/ряд=порядок листьев, безье правый порт → левый порт;
  fit-масштаб ≤ 1; hit-тесты; машина состояний §6.4 Loading → Ready с
  чипом Stale; лоадер — ротация 12 подписей AC-1.2/У5); `app.rs` —
  open_explain (сессионный кэш AC-3.3: тот же корень + ревизия —
  мгновенный Ready ≤ 1 с/G1, иначе Loading + фоновая сборка — натив
  поток G5, wasm синхронно), close_explain (снапшот → кэш),
  on_explain_click (✕/чип/крошки/узлы; клик по фону — закрытие),
  result_band_root_at (F-1/AC-1.1 — клик по полосе D: фолбэк-триггер;
  AC-1.4 — константа открывает панель одного узла), explain_hover_pill
  (Closed → Hover, hover-only), explain_frame (модальный проход кадра
  паттерном main stage: в Loading затемнения нет — У5; в Ready — дерево
  безье + карточки «заголовок/значение/формула/адрес», полоса рода
  (Calc — акцент, Leaf — EXPLAIN_LEAF, терминалы — ошибка/предупреждение),
  бейдж «+N глубже», футер-статистика), explain_chain_focus (F-4:
  подсветка цепочки через механизм FocusView T23 — узлы/рёбра дерева →
  FocusSet, пучки — OR-семантикой рендера); F-5/F-10: панель и подсветка
  рендерят один снапшот; stage ↔ explain взаимоисключительны (открытие
  stage закрывает панель, снапшот остаётся в кэше — У10). `scene.rs` —
  `SceneState.revision` (монотонный счётчик в recompute_flow: чип при
  любом изменении — канвас/MCP/файл/подмена/Apply, без исключений — У3)
  и `SceneState.flow_cycle` (AC-2.4 — сборка в Cycled-режиме). `canvas-core`
  — `Settings.explain_depth_limit` (AC-2.3: дефолт 3, кламп 0..12,
  dropdown [2/3/4/без ограничения] в FR-039); токены
  EXPLAIN_LEAF/EXPLAIN_LEAF_LIGHT + `ThemeColors.explain_leaf` (dark/light,
  пресеты — по яркости фона; AC-3.4 контраст ≥ 3:1). i18n — 34 ключа
  RU/EN. Скоуп X2: триггер — полоса D итога ноды (построчные ряды —
  вместе с X3 what-if, где нужна адресация строки); крошки — клик по
  мета-строке к корню вида (полные чипы — UX-шлифовка X6); У2-вспышка
  канвас→дерево — X6. Гейты: fmt, clippy `-D warnings`, cargo test
  (1115 нативных), token_lint, wasm_gate (ступени 1–3) — зелёные.
  Демо-точка владельцу (Q6) — готова.

## Источники истины

- `docs/prd/prd-0007-calc-chain-explain.md` — требования, CJM, roadmap, DoD.
- `crates/canvas-core/src/flow.rs` — `propagate_with_lines_data` (`:375`),
  `inbound_slots_with_lines` (`:555`), `param_spills` (`:698`),
  `unmapped_inputs_with_data` (`:758`), `value_path` (`:870`),
  `creates_value_cycle` (`:909`), `WhatIfOverrides` (`:235`).
- `crates/canvas-core/src/expr.rs` — `Value` (`:304`), `Expr` (`:385`),
  `NumiLineKind`/`line_kind` (`:1694/1719`), `eval_lines_with_env` (`:1277`).
- `crates/canvas-core/src/model.rs` — `Node` (`:203`), `Edge` (`:501`).
- `crates/canvas-core/src/dataref.rs` — `display_ref` (`:108`) — F-3 (X2).
- `crates/canvas-core/src/bundles.rs` — `dominant_edge` (`:360`) — AC-3.2.
