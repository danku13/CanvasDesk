## T8 (Свяжи / Edges) — Финальный план реализации

### 📊 Текущее состояние: **~95% готово**
Все основные требования T8 из TASKS.md уже реализованы в кодовой базе:
- Модель Edge + Canvas operations (model.rs)
- Геометрия: порты, Безье, тесселяция, hit-test (edgegeom.rs)
- Рендер: линии-точечки, стрелки, порты hover, резиновая линия (cards.rs)
- Инлайн-редактирование лейбла (edit.rs + renderer.rs)
- Ввод: drag от порта, drop → edge, Del, hover, dbl-click (main.rs)
- Юнит-тесты есть в canvas-core/tests/edgegeom.rs и canvas-render/src/cards.rs

---

### ✅ Чеклист оставшейся работы

#### 1. Интеграционные тесты — **Новый файл `canvas-app/tests/integration_edges.rs`**
- [ ] `test_create_edge_via_port_drag` — hover → drag port → drop → edge в модели + рендер
- [ ] `test_edit_edge_label_double_click` — dbl-click на линии → EditingSession → Enter → label сохраняется
- [ ] `test_delete_edge_via_del_key` — клик по линии → выделение → Del → edge удаляется
- [ ] `test_cascade_delete_node_removes_edges` — удалить ноду → каскадно удаляются её edges

#### 2. Edge-case юнит-тесты в `canvas-core/tests/edgegeom.rs`
- [ ] `test_self_loop_geometry` — edge от ноды к себе же: корректная геометрия/рендер
- [ ] `test_all_16_side_combinations` — все комбинации fromSide→toSide рендерятся правильно
- [ ] `test_port_hitzone_at_extreme_zooms` — port_at работает на zoom 0.05..4.0

#### 3. HUD улучшение
- [ ] Добавить `visible_edges` / `total_edges` в `FrameStats` (renderer.rs)
- [ ] Обновить `hud_text()` в main.rs: показать "edges видно X/Y"

#### 4. Ручная приёмка (на dev-машине Win11 25H2 build 26200)
- [ ] Создать 2 файловые ноды
- [ ] Hover → 4 порта видны
- [ ] Drag от правого порта → резиновая линия
- [ ] Drop на вторую ноду → edge + стрелка
- [ ] Dbl-click на линии → редактирование лейбла "blocks"
- [ ] Enter → label виден на линии
- [ ] Клик по линии → выделена (акцент, толще)
- [ ] Del → edge удалён
- [ ] Перетаскивание ноды → edge тянется за ней
- [ ] Удаление ноды → edges каскадно
- [ ] Save → restart → edges на месте с fromSide/toSide
- [ ] Открыть .canvas в Obsidian → связи отображаются

---

### ⏱️ Оценка: ~4-6 часов
- Интеграционные тесты: 2-3ч
- Edge-case тесты: 1ч  
- HUD + ручная приёмка: 1-2ч

---

### 🎯 Критерии готовности T8 (SPEC §10 / TASKS M2)
> **M2 done:** заметки редактируются инлайн, **связи рисуются и сохраняются**, drop папки из Explorer раскладывает файлы, переименование файла в Explorer обновляет карточку за < 1 с

Все пункты про связи — **готовы к приёмке** после написания тестов.