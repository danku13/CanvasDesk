# Changelog скиллов CanvasDesk MCP

Формат: версия пакета, дата, изменения. Правила ведения —
[UPDATE-PROTOCOL.md](UPDATE-PROTOCOL.md).

## v4 — 2026-09-25

FR-072: разделение заголовка ноды и текста тела. Новое поле
`canvasdesk.title` — явный заголовок карточки, не выводящийся из первой
строки текста. Реестр — 41 инструмент (состав не менялся, расширены
параметры и семантика трёх).

- `canvasdesk-model-build` (v2) — правило «заголовок карточки»: title в
  `node_create_note`/`node_edit`, сброс null к legacy-фолбэку.
- `references/tools.md` — `node_create_note` (+title), `node_edit`
  (+title, null — сброс), `nodes_search` (поиск по заголовку).
- README — версия пакета v4 (счётчик инструментов без изменений).

## v3 — 2026-09-24

FR-066 (M5/S3, ADR-0008 волна S): новый инструмент `monte_carlo_run` —
Monte Carlo/QMC-прогон модели с распределёнными параметрами
(N ≥ 10⁴, mode qmc/mc, сид-воспроизводимость) и квантили P50/P90/P99
результатов + анализ узких мест на хвостовом P90. Реестр — 41
инструмент (native; wasm-сборка отдаёт 40 — monte_carlo_run за фичей
`qmc`, не собирающейся на wasm32, FR-066 §5.8).

- `canvasdesk-model-verify` (v3) — раздел «monte_carlo_run — квантили
  при неопределённости» (сигнатура, формат ответа, лимиты, seed в
  отчёты), шаг 6 порядка верификации.
- `references/tools.md` — строка инструмента в группе «Вычисление
  и проверка» (7), счётчик заголовка 39 → 41 (устаревший).
- README — счётчик 40 → 41.

## v2 — 2026-09-22

PRD-0007 X6 (FR-048, F-9): новый инструмент `explain_number` —
объяснение цифры текстом (линейная развёртка дерева происхождения
с адресами и значениями; ответ render:"text" — мост отдаёт text как
content text). Реестр — 40 инструментов.

- `canvasdesk-model-verify` (v2) — раздел «explain_number — объяснение
  цифры текстом» (сигнатура, формат ответа, границы с `lineage`),
  шаг 5 порядка верификации — готовый текст для ответа пользователю.
- `references/tools.md` — строка инструмента в группе «Вычисление
  и проверка» (6).
- README — счётчик 40, назначение скилла verify дополнено.

## v1 — 2026-09-22

Начальный пакет: 4 скилла, синхронизированы с реестром из 39
инструментов (commit 17b9134, main).

- `canvasdesk-mcp` — подключение и разведка: транспорт stdio
  (offline/reconnect/протоколы), инварианты (value-связи,
  MCP-видимость = UI, каскад Р-1, undo-дисциплина), инструменты
  чтения, карта скиллов, подводные камни.
- `canvasdesk-model-build` — рецепт сборки (разведка → ноды →
  value-связи с адресацией портов → атомарный батч graph_apply →
  визуальная доводка), таблица операций и кодов ошибок, эталонный
  пример Instagram MVP (ADR-0005: 12 нод, 10 рёбер, оракулы ±1 %).
- `canvasdesk-model-verify` — flow_recalc (структура ответа: value/
  outputs/lines/spilled/autoRows), lineage (дерево, kind, via),
  flow_cycle_check, graph_validate (таблица кодов E-*/W-*),
  analyze_bottlenecks (severity/пороги), порядок верификации.
- `canvasdesk-whatif` — 9 инструментов сценариев, дельты,
  дисциплина apply/reset, типичный сеанс, ограничения.
- `references/tools.md` — полный каталог 39 инструментов по группам.
- Контракт-тест `skills_sync` (canvas-mcp): полнота каталога,
  покрытие скиллами, счётчик README, валидность call-позиций.
